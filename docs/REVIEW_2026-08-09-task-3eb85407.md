# REVIEW — METTL5 任务复跑审查 + 前端事件回放修复（2026-08-09）

> 审查对象：`task_3eb85407-eee2-4ff2-8bf3-d481c1277d55`
> 任务目标：下载清洗 METTL5 在胰腺癌癌组织与癌旁组织的转录组学差异（与 REVIEW_2026-08-05 同主题复跑）
> 执行模型：Qwen（DashScope），live 模式
> 结论：**流程基本顺畅但存在显著无效消耗（工具 schema 冲突重试 + 两次 GEO 同因失败）；最终产物为 GDC 单样本表达表，无法回答差异分析目标；run 因 429 配额耗尽失败。前端 session 回放崩溃（operation_progress 未知事件类型）已修复。**

---

## 1. 执行轨迹（events.jsonl，1710 事件）

| 阶段 | 工具调用 | 结果 |
| --- | --- | --- |
| 发现 | find_skill×4 → search_pubmed/gdc/xena/geo | ✅（search_gdc 首次因传 `query` 缺 `term` 失败，重试后通过） |
| 发现 2 | search_gdc(term)/xena/geo | ✅ |
| describe | describe_gdc / describe_geo(GSE141873) / describe_geo(GSE154593) | ⚠️ describe_gdc 传多余 `data_category` → schema 拒绝 |
| 下载 | download_gdc×3 / download_xena×3 / download_geo | ⚠️ download_gdc 两次因多余参数拒绝、download_xena 两次因 `dataset`/`cohort` 拒绝 |
| 检查 | read_file_head / search_file(METTL5→命中 ENSG00000138382) | ✅ GDC 文件确认基因级、含 METTL5 |
| Build 1 | `build_paad_mettl5_geo`（GSE141873） | ❌ NO_DATA：canonicalize 全行 `unknown_unit`（`arbitrary_unit`） |
| Build 2 | `build_paad_mettl5_gdc` | ❌ NO_DATA：parse 错文件（expression table 表头不符） |
| Build 3 | `build_paad_mettl5_gdc_v2`（单样本验证） | ✅ **SUCCEEDED**（60,660 行 × 1 样本，已发布） |
| Build 4 | `build_paad_mettl5_geo_gse27890` | ❌ NO_DATA：与 Build 1 同因（canonicalize 零行） |
| 收尾 | list_files / read schema | ✅ 检查发布产物 |
| 终态 | run_failed | ❌ **429 insufficient_quota（token 配额耗尽）** |

**4 个 build 全部位于单次 run 内**（run_5466fb60）；`gdc_v2` 为唯一成功发布（25 MB primary.csv + 完整 manifest/validation/provenance）。

## 2. 流程顺畅度审查：存在显著不必要的重试

1. **工具 schema 冲突重试 6 次**（直接浪费 token）：
   - `search_gdc`：LLM 用 `query`，schema 要求 `term`（一次失败）
   - `describe_gdc`：LLM 传 `data_category`，schema 禁额外属性（一次失败）
   - `download_gdc`：LLM 连传 `data_category`/`workflow_type`，schema 禁额外属性（两次失败）
   - `download_xena`：LLM 传 `dataset`（错名）→ `cohort`（禁额外属性）（两次失败）
2. **GEO 失败模式重复**：Build 1 与 Build 4 以完全相同根因（canonicalize 全行 `unknown_unit`）失败；Agent 未先诊断 Build 1 的 rejected.csv 就直接换数据集重试。
3. **最终 429 收尾失败是必然结果**：1556 条 reasoning delta + 上述无效调用把配额耗尽，run 在产出最终 summary 之前中断。

## 3. 产物对照赛题（PROBLEM.md）

| 评价维度 | 判定 | 说明 |
| --- | --- | --- |
| 数据查找完备性 | ⚠️ 部分 | 多源发现（GEO/TCGA-GDC/Xena/PubMed）已做；但最终只整合 **1 个 GDC 样本** |
| 来源可追溯性 | ✅ | 22 列 schema、逐行 source_id/asset_id/行号溯源、provenance closure 完整 |
| 清洗整合可靠性 | ❌ | **最终产物 60,660 行全部来自单一肿瘤样本**，无癌旁对照 → 无法做差异分析 |
| 输出格式可用性 | ✅ | gene-sample 长格式、gene_id=ENSG、value 合规，下游可直接过滤 |

**核心缺陷**：任务目标是"癌组织 vs 癌旁组织差异"，但最终成功发布的 build 是"GDC 单样本表达验证"（`objective` 自降级为单样本）；两次携带配对 tumor/normal 的 GEO 数据集（GSE141873/GSE27890）全部 NO_DATA。**差异分析目标未达成，仅得到一张单样本基因表达表。**

## 4. 对照 REVIEW_2026-08-05 的遗留问题验证

| REVIEW 建议 | 本次验证 |
| --- | --- |
| §3.1 P0 GEO probe→gene 映射 | **仍复现**：两次 GEO 失败根因即 probe 平台 + 单位声明问题（`arbitrary_unit` 不在 profile allowed_units → canonicalize 全拒）。probe→gene 映射（GPL 注释）在工具路径仍未接线 |
| §3.2 P1 单基因优先 TCGA | **部分生效**：Agent 确实走 GDC RNA-seq 基因级数据，但只用 1 样本、未做分组 |
| §3.3 P1 plan HITL 超时 | 本次未触发（无 plan 确认） |
| §3.4 P2 describe_geo vetting | describe_geo 已用（GSE141873/GSE154593），但未用于单位判定 |
| 新增 | **工具 schema 传参冲突**是本轮新增的显著 token 消耗源；429 配额耗尽导致已发布产物未登记到 task 终态（`current_publication_id=null`，UI 显示 run failed） |

## 5. 前端 Bug：`Unknown event type "operation_progress"`（已修复）

- **根因**：REST 事件回放解析链（`frontend/src/lib/apiResponseParsers.ts` 的 `assertEventType` 白名单）缺失 V2 阶段新增的 `operation_started/progress/completed/failed` 四类事件；WebSocket 通道（`transport.ts`）一直支持，所以实时对话正常、只有打开历史 session 回放（`GET /tasks/{id}/events`）时崩溃。
- **修复**（三个解析文件 + 测试）：
  - `apiResponseParsers.ts`：`assertEventType` 白名单 + `RUNTIME_EVENT_TYPES` 加入 4 类 operation 事件
  - `eventParsers.ts`：payload 路由集合补 4 类
  - `eventParsersRuntime.ts`：实现 4 个 operation payload 解析分支（含默认 `succeeded` 状态、ErrorDetail 复用）
  - 新增 11 项回归测试（含复现原报错的 `parseEventPage` 完整 envelope 回放），36+115 前端测试通过

## 6. 本轮已修复的高优先级问题

1. **GEO fail-fast（spec 预检）**：`SpecValidator` 对 geo.expression.v1 binding 的声明参数（`expression_unit` / `value_semantics` / `value_scale`）与 normalization profile 的允许集交叉校验，返回结构化 reason code（`unknown_unit` / `unknown_semantics`）。Agent 传 `arbitrary_unit` 这类 canonicalizer 必然拒绝的单位时，`validate_dataset_build_spec` 立即报错并列出允许值，不再静默跑完整条链后产出零行 NO_DATA。新增 2 项测试。
2. **工具 schema 对齐**（消除本次 6 次重试）：
   - `search_gdc`：接受 `query`（与 pubmed/geo/xena 对齐），`term` 保留为兼容别名
   - `describe_gdc`：接受可选 `data_category` 过滤
   - `download_gdc`：接受可选 `data_category` / `workflow_type` 过滤（拼入 /files 查询）
   - `download_xena`：接受可选 `cohort`（回显在响应中）
   - 新增 4 项测试

## 7. 遗留与建议

- **P0（未修，需独立任务）**：GEO probe→gene 映射接线——GPL 注释下载 → `mapping_paths`/`mapping_assets` 注入 runner，使探针平台数据集能产出基因级或 probe-primary（`gene_expression.probe_release.v1`）产物。当前 spec 预检只保证"快速失败"，不保证 GEO 探针数据可用。
- **P1**：AGENTS/INSTRUCTIONS 应引导"单基因差异分析需 ≥2 组样本（tumor vs normal）"，避免再次产出单样本"验证"型产物。
- **P1**：429 配额耗尽的收尾体验——run failed 时已发布产物未关联到 task（`current_publication_id=null`）；建议发布先于终态的窗口处理（LEFTOVERS C1a）。
- 本次 run 的磁盘产物（4 个 build）可作 regression fixture 候选；重跑时注意 GDC 文件为真实下载。
