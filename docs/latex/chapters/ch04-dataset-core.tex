\chapter{确定性数据核心：八阶段流水线（Dataset Core）}\label{ch:dataset-core}

\begin{wrapfigure}{l}{0.55\textwidth}
  \centering
  \includegraphics[width=\linewidth]{figures/biomed-qagent-core-deterministic-flow.pdf}
  \caption{Dataset Core 确定性数据流详图}
  \label{fig:core-deterministic-flow}
\end{wrapfigure}%

Dataset Core 是系统唯一的数据信任边界。它将数据处理固定为八个阶段，从采集、解析、规范化、兼容性检查、整合、多表组装、校验到发布（acquire、parse、normalize、compat、integrate、derive、validate、publish）。每个阶段都有明确的输入、输出摘要和失败含义，也有执行完成的凭证，说明该阶段做了什么、产生了哪些文件及各文件的摘要。

Agent 的自主决策面见 \ref{sec:agent-autonomy} 节。决策进入 Core 后，执行路线固定，正式数据值由注册操作产出。动态数据家族（Family）允许 Agent 提供的变换代码在一个受限执行区内产生候选字节；这些候选要成为正式数据，必须依次通过准入检查、完整性校验（Validation）、多表产品评估（ProductAssessment）与发布器（Publisher）的逐级检查。准入检查核对输入、输出与摘要是否精确闭合，任何一级不通过即整体拒绝。动态家族扩展的是“能读什么”，不扩大“能写什么”。

详图展开 Core 内部的两条路线。A 路线使用已注册数据家族的固定操作骨架；B 路线处理动态规格：变换方只交付候选字节，隔离区和 Core 再通过重新计算摘要、检查闭合条件等方式统一校验。变换方不能选择校验、评估或发布模块，这构成 \ref{sec:contributions} 节所述运行时表结构准入控制的机制基础。

变换产出的表头哪怕只与声明相差一个字段，隔离区（quarantine）也会以 \code{OUTPUT\_\allowbreak{}HEADER\_\allowbreak{}MISMATCH} 拒绝该候选。占位符空壳是内容筛查上线前唯一一次穿透到发布的案例，上线后未再发生。

八个阶段运行在共同的可靠性基座上（\ref{sec:reliability-base} 节）。此外，系统用明确的结束状态区分“运行完成”和“正式发布”，拿不准就拒绝。终态分为 Rejected、NO\_\allowbreak{}DATA、Failed 与 Cancelled 四类。以下按八个阶段依次说明。

\section{阶段 1：正式采集与来源资产登记（acquire）}

Agent 的来源发现（第三章）拿到 URL 后，正式路线不会直接信任 Agent 工作区中的任意文件。Core 的采集提供者先确定请求的 URL、方法、请求头、媒体类型和允许参数，再由统一下载设施执行网络访问。该设施实施 URL/\allowbreak{}DNS 策略、大小与超时限制、重试、断点续传和内容缓存，以降低服务端请求伪造（SSRF）、无限下载和重复获取等风险。

文件写入任务专属的来源资产目录后，来源资产注册表流式计算 SHA-256 并生成内容身份。它同时绑定任务与资产角色、文件字节数、媒体类型与 SHA-256、来源 ID 与规范编号、提供者与采集实现的身份摘要，并在支持时记录快照身份或修订号等版本证据。目录逃逸、符号链接和跨任务资产复用一律拒绝。

因此，“来源资产”不是一个本地路径，而是任务、来源、内容与采集实现共同绑定的输入证据。

\section{阶段 2：异构数据解析（parse）}

\subsection{结构化与半结构化来源}

正式静态路线使用已注册的解析器（Adapter）。解析器将来源特有的 CSV、JSON、XML、XLSX、SOFT、矩阵或 API 响应转换为数据批次（DataBatch），并同时输出解析统计、被拒绝的记录和溯源定位信息。遇到来源格式异常时，解析器应失败，或将问题写入审计记录，而不是让 Agent 猜测缺失字段。

表达数据的解析器需要识别样本列、实体标识、测量值和表达含义。例如 GEO 既可能提供 gene-level 结果，也可能只提供 probe-level 矩阵；系统不会在缺少可信注释时直接把探针当成基因。GDC 与 Xena 的输入形态不同，但都需要转换到目标表达表结构后才可合并。

与动态家族相对，注册式多表数据家族将每个来源表映射到明确的表定义，并在后续组装阶段检查关系（多表示例见 \ref{sec:stage6-derive} 节）。

\subsection{PDF 表格}

PDF 解析保留页面和位置证据。对于可提取文本的表格，可依据文本块位置聚类形成行列，并记录 page、bbox、caption 或降级警告。无框线表格、合并单元格、旋转页面和跨页表头仍是启发式解析的难点，这类复杂度应在 gold 案例设计中单独标注，解析结果也要人工核对。

\subsection{图表与视觉模型}\label{subsec:chart-vlm}

图表工具采用多层降级策略。系统优先通过 \code{qwen3.8-flash} 理解页面图像或嵌入图像；失败后尝试解析 PDF 表格；再次失败时，只提取标题（caption）作为有限信息。模型抽取结果必须携带模型名；图表中每个数据点的抽取结果还必须包含置信度及其判定原因。其可信级受 \ref{sec:stage7-validate} 节所述确定性上限的约束，不会因为模型自报 high 就自动成为正式真值。

就规范侧而言，已落地的链路是：VLM（视觉语言模型）、PDF 表格或标题抽取的结果先作为结构化图表证据（chart-evidence）进入系统，形成带点级取值与来源绑定的证据资产，再进入生产环境的 bioactivity\_\allowbreak{}measurement 数据家族。corrected gold6 的正式发布包含活性值、实验、论文、补充资产、图系列与图点 6 个 CSV 表，另有 schema、provenance 与 ProductAssessment，共 9 件 Artifact。组装前，系统逐点核对来源并复核证据，无法确认的图表数据不纳入正式数据。复核未通过时，系统写入结构化 \code{chart\_\allowbreak{}evidence\_\allowbreak{}gate} 检查结果并阻止发布，不会让任意工作区 CSV 直接获得正式发布权。人工修正保留原始值，并记录 human\_\allowbreak{}correction 处理步骤；但本次 corrected gold6 的 5 次 HIL 是 3 次 credential 授权和 2 次发布验收，不应写成低置信度点值人工修正。

系统用确定性规则检查轴与图例，不以模型自报为准。\code{chart\_\allowbreak{}series} 表记录轴名、轴单位与刻度和图例文本，并须声明轴/\allowbreak{}图例校验状态（clear /\allowbreak{} unclear /\allowbreak{}
human\_\allowbreak{}validated）；声明为 exact 的数值点一旦对应轴或图例含义 unclear 即直接判失败；系列允许省略点数据的唯一条件是轴或图例状态明确标为 unclear。这条链路已有点级的 gold 案例完成测试。无论采纳（accepted）还是人工修正（corrected），两条路径都产出正式发布物并逐件重算 SHA-256；未经复核或缺表的运行不产生任何发布物。

\section{阶段 3：含义规范化与缺失、重复处理（normalize）}

解析后的来源记录仍不能直接拼接。规范化器（Canonicalizer）依据目标表结构完成类型转换、字段命名统一、实体标识表达和必要的值标准化。核心原则是：只有目标表结构和规范化配置明确授权的变化，才可以自动执行。

字段对齐包括来源列名到规范字段的映射、类型转换、缺失标记统一、实体 ID 与命名空间表达、单位与测量含义检查，以及 probe-to-gene 等外部映射资产转换。规范化过程保留原始写法与规范值之间的血缘（lineage）。

缺失与重复按类型分层处理：必填字段缺失时拒绝或阻断，可选字段缺失则保留为空并计入完整性统计；完全重复按确定性键去重；主键重复且值一致时合并血缘、保留多来源。每类处理都保留对应证据，如定位信息与来源集合，以便后续复核。

这些规则在 corrected gold8 中体现为：正式 Publication 只包含 openFDA FAERS 的断言表与研究表；其他请求维度仍是工作区 staging，不能据此声称整个 DILI 多源请求完整发布。未取得的来源不以零值补齐。

原始写法与规范值并列保存是当前规范化设计；具体案例中的字段与行数则必须以对应 Publication manifest 为准。corrected gold7 的风险位点和变异--基因映射属于两个独立 Publication，corrected gold9 的正式主表为 assertion records，不能沿用旧版“三张实体表”叙述来描述本次主评测。

实体身份对齐同样采用确定性机制。化合物以 InChI Key 精确匹配建立交叉映射表，例如 ChEMBL 与 PubChem 的互查按 exact\_\allowbreak{}inchi\_\allowbreak{}key 方法匹配，匹配不上时显式标记为冲突，而不强行合并。基因符号归一有内置确定性映射表，也可使用注册的外部规范资产，如 gold9 使用的 HGNC current。探针到基因的映射依赖逐平台注释资产，未映射探针保留原命名空间，不静默丢弃。

\section{阶段 4：兼容性检查（compat）}

兼容性检查是解析和规范化产物进入整合前的最后一道静态检查。它核查各批次在数据家族、表结构、行层级、实体层级、单位、含义和尺度上是否可以合并，并据此生成确定性的合并域键。合并域键由两部分共同决定。一是可计算的测量身份，即值的含义、尺度与单位；二是多维语义，包括模式、行层级、物种、特征命名空间、测量类型、规范化状态与参考版本等。任何一维不一致，都不能进入同一合并域。

未知单位、未知值含义或不受支持的数值尺度不会被自动修正；单位不一致时，只在有注册转换公式时换算，公式只接受四种线性安全形式（value、value±o、value×f(±o) 与 value/\allowbreak{}d），其中 value 为原值，o 为偏移量，f 为正倍率，d 为正除数；没有注册规则时阻断，或经人工确认请求研究者选择合法的修正。

两个案例可以说明这一原则。坐标基准转换不属于任何线性安全形式，系统不做 GRCh37 到 GRCh38 的自动换算，而是双列并存，交由外部权威核验。gold7 的 \code{position\_\allowbreak{}grch38} 列取 dbSNP 核验值，与补充材料列 \code{position\_\allowbreak{}grch38\_\allowbreak{}supp} 并列，两个来源不一致时直接可见。gold9 中 ClinGen 的序数分类与 ClinVar 的记录计数测量身份不同，各自成列，不进入同一合并域。

\section{阶段 5：多源整合与冲突审计（integrate）}\label{sec:stage5-integrate}

对同一规范表结构的批次，整合器按注册的合并策略合并，并在最终来源权威行（source-of-record）上重新汇总血缘和置信度。合并时的冲突按注册策略处理：主键重复且值冲突时保留来源权威行并写冲突审计，必要时阻断；实体身份冲突不强制合并而保留交叉映射表；统计异常作为警告或人工核查信号，不默认自动改值。

整合层优先做“可复核的记录”，而不是“替研究者定案”。gold7 是实测样本：\code{locus} 表以 rsID 为合并键，GWAS Catalog 关联与补充材料 75 位点表在主键上重叠、
值一致时合并血缘并保留双来源标记（\code{in\_\allowbreak{}supp\_\allowbreak{}75\_\allowbreak{}loci}）；同一坐标的两个来源值以双列并排保留，而非静默取舍。这些字段对齐与冲突记录由规范化、兼容性阶段与整合器的确定性规则共同完成，冲突审计随产物交付，无法自动裁决时才发起人工确认（\ref{sec:hil} 节）。

真正需要“二选一”的少数同键冲突，整合阶段采用先到优先（first source wins）作为可复现的终局裁决；最终取哪个值为真，仍显式留给研究者与人工确认判断。

\section{阶段 6：多表组装（derive）}\label{sec:stage6-derive}

对于需要多实体关系的数据，数据家族的组装阶段生成多表候选，并检查主键、外键、基数和表角色。\footnote{主键：表中每行的唯一标识。外键：表中指向另一张表主键的字段，如测量表中的化合物编号指向化合物表。基数：两表间一条记录与另一表几条记录的对应关系，即一对一、一对多或多对多。表角色：各表分别承担记录测量值还是提供规范身份。}

单表与多表是规格层的可选形态。需求本身是单一层级观察时交付单表，例如 gold8 的 FAERS 不良事件计数；需求横跨多个实体层级时才拆分多表，例如 gold7 拆出研究、位点与变异-基因映射多张表，gold9 拆出基因、疾病与证据结构。以生物活性数据为例，测量值表（activities）按声明的外键关联实验条件表（assays）与化合物规范身份表（compounds），并辅以靶点表、来源表与化合物交叉映射表等（图~\ref{fig:bioactivity-fk}），避免把不同层级的观测压进重复严重的宽表。如果将实验条件并入测量值表，同一实验的上千条测量记录就必须逐行重复相同的实验条件字段。

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\textwidth]{figures/biomed-qagent-bioactivity-multitable-fk.pdf}
  \caption{生物活性数据的多表组装：activities 经外键多对一关联 assays 与 compounds}
  \label{fig:bioactivity-fk}
\end{figure}

多表发布清单记录表、关系、溯源引用和置信度引用。下游按清单声明的显式关系连接各表，而不是凭列名猜测连接键（join key），例如测量表按清单声明的 \code{compound\_\allowbreak{}id} 外键关联化合物表，而不是依据两表同名的 \code{name} 列自行连接。若下游统计流程要求单表输入，研究者可在目标表结构中将交付声明为单表层级，gold8 就是这种做法。

\section{阶段 7：校验、置信度与产品评估（validate）}\label{sec:stage7-validate}

校验配置（Validation Profile）会在发布前检查候选结果，范围包括表结构与字段类型、必填字段完整性与允许缺失率、主键唯一性与外键基数、实体标识命名空间与原始写法保留（token preservation）、单位与数值尺度及允许含义、溯源与来源定位覆盖、置信度记录与最终行对齐、产物摘要闭合，以及可复现所需的输入与实现身份。

多表产品评估（ProductAssessment）从结构、关系、标识、溯源、置信度和可复现性六个维度评价产品。无阻断项时判定为“可发布”（publishable）；只存在可复现性阻断项时至多为“已验证”（validated）；存在内容维度的阻断项时判定为“不完整”（incomplete）。其中，可复现性阻断指缺少复算所需的输入、实现身份或摘要；内容维度的阻断项指结构、标识、单位或溯源等维度不闭合。
这使得“CSV 能打开”与“数据产品含义上闭合”不再是同一件事。

置信度按证据类型受到上限约束。确定性、受版本约束的解析可以获得包括 high 在内的较高可信等级；VLM、LLM、OCR 和网页抽取等非确定性来源最高封顶为 medium，并可按记录进入人工审核。系统关注的是“该记录由何种证据和处理产生”，而不是让模型自由输出一个看似精确的概率。

验证结论还与明确的结束状态绑定。Rejected、NO\_\allowbreak{}DATA、Failed、Cancelled 四类结束状态相互独立，并遵循“拿不准就拒绝”原则。空主数据、兼容性失败或验证失败的结果一律不可发布；“运行完成”也不等于“发布物生成”。

\section{阶段 8：不可变发布与消费时重验（publish）}\label{sec:stage8-publish}

发布器将质量检查合格的候选复制到独立目录，生成发布清单（Manifest），并为每个文件记录相对路径、媒体类型、字节数和 SHA-256。典型正式产物包括主数据集与辅助数据集 CSV、表结构、溯源、审计/\allowbreak{}校验报告，以及发布清单和发布回执。

发布采用临时目录与原子切换。发布前，系统检查执行锁与代次围栏（表~\ref{tab:reliability-base}），防止超时或取消后的旧操作晚到并覆盖新结果。产品 API 在读取时重新验证发布清单和文件摘要；若文件被修改或损坏，API 返回错误，不继续展示该文件。

这里的“不可变”是指正常 API 只追加发布、不覆盖旧版本。读取端按清单重新计算摘要，可以检出意外或不完整篡改；但系统不提供防止同一 OS 账户完全重写文件系统的密码学防篡改能力。

\section{持久化人工确认与可恢复执行（Durable HIL）}\label{sec:hil}

人工确认（Human-in-the-Loop, HIL）不是系统的第一反应，而是在确定性规则无法解决问题时使用的兜底环节。遇到模糊字段映射、未知单位或低置信度图表点等情况，Core 先尝试注册的确定性规则和候选推理；只有无法安全定案时，才创建结构化请求。例如，单位换算先查询注册公式表，命中后自动应用，不进入人工确认；只有不存在可用规则时才暂停。图表审核也只针对低置信度记录发起。
请求携带逐项候选值（proposed\_\allowbreak{}value）、置信度（confidence\_\allowbreak{}level）、相关记录、证据摘要以及任务/\allowbreak{}运行/\allowbreak{}需求身份。前端以“在候选与证据概要上选择”为主，以自由修正为辅（approve /\allowbreak{} reject /\allowbreak{} correct）。只有证据摘要和任务身份都匹配时，响应才能恢复原操作。

典型场景包括字段映射存在多个候选、未知单位或测量含义、探针到基因映射的歧义、低置信度图表点的确认，以及冲突记录需指定保留策略等。

按审批对象划分，人工确认覆盖三类：工具许可与凭据授权（approve/\allowbreak{}reject）、数据审核（字段映射、单位、图表与网页证据）和发布验收。对于需要验收的高风险产品（动态发布、图表证据、浏览器证据接受），发布验收不可绕过；只有人工 accept 后系统才执行发布。普通静态数据发布不统一要求 publication-acceptance HIL，这与“Core 独占发布权”相互配合。

审批档位按对象配置。每类审批范围可设为人工审批（\code{human\_\allowbreak{}review}）、LLM 初审（\code{llm\_\allowbreak{}pre\_\allowbreak{}review}，仅不通过项上报人工）或显式自动通过（\code{auto\_\allowbreak{}approve}）。初审不通过或模型调用失败时一律回退人工（fail-safe）；涉及发布验收的对象强制人工审批，配置其他档位会被直接拒绝。低风险确认可以配置自动放行，高风险场景始终由人工处理。

请求和决策均落盘，进程重启后可以确定性恢复：系统持久化审批挂起时的原始工具调用，恢复时以原调用重放并由执行器从检查点续跑，零 LLM 参与；主机重启后无法确定性续跑的发布验收直接判整次运行失败，恢复时已失去对象的挂起权限请求一律按拒绝处理。对于数据更正，系统重新执行受影响阶段并发布新版本，通过 supersedes 关系保留旧版本历史。

需要澄清的是，检查点复用是一项运行时机制，但个别监督会话在 permission stop 后重新 adopt，不自动等价于 Dataset Core 阶段检查点命中。corrected gold9 的证据确认多次权限挂起后继续运行并最终发布，却不足以单独证明“中断后未重跑全链路”；后者应由明确的 checkpoint/replay 记录支撑。

\section{可靠性基座}\label{sec:reliability-base}

八个阶段运行在共同的可靠性基座上。表~\ref{tab:reliability-base} 逐项给出机制定义与运行日志中的实测记录：

\begin{xltabular}{\linewidth}{>{\hsize=0.6\hsize}L>{\hsize=1.0\hsize}L>{\hsize=1.4\hsize}L}
\caption{可靠性基座：机制与实测记录}\label{tab:reliability-base}\\
\toprule
\textbf{机制} & \textbf{一句定义} & \textbf{实测记录} \\
\midrule
\endfirsthead
\toprule
\textbf{机制} & \textbf{一句定义} & \textbf{实测记录} \\
\midrule
\endhead
执行锁 & 同一任务同一需求的发布互斥；目录原子锁，单机多进程有效 & 机制由运行时测试覆盖；主评测报告不以历史主机重启次数作统计 \\
代次围栏 & 发布前复核执行租约，超时或已取消的旧执行不得晚到覆盖新结果 & 机制由执行租约与取消路径测试覆盖 \\
检查点与重放 & 已完成且输出摘要一致的操作按重放身份复用 & 需要独立 checkpoint/replay 事件证明；permission resume 不作替代证据 \\
超时与取消 & 每个操作有界，取消即时生效 & 运行时测试覆盖超时与取消；历史缺陷不混入 corrected cohort \\
事件溯源 & 全部操作追加事件日志，状态可由日志重建 & qwen3.7-plus 三轮谎报“整合完成”被事件流的终态分类标为未完成 \\
Durable HIL & 审批请求与决策落盘；恢复以原调用重放、零 LLM 参与 & 机制与三档审批见 \ref{sec:hil} 节 \\
内容摘要与重验 & 逐件 SHA-256；读取端每次读取重算，不匹配即拒绝展示 & 正式案例全部产物经监督程序逐件重验一致（\ref{sec:eval-protocol} 节） \\
\bottomrule
\end{xltabular}
