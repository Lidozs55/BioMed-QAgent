%% =====================================================================
%% smoke.tex — 最小模板编译冒烟测试
%%
%% 只覆盖：chapter/section/subsection、lstlisting、enumerate、
%%         keypoint、resultbox。
%% 用于模板 clean-compile 基线，不涉及 25 页主报告。
%%
%% 编译：latexmk -xelatex -outdir=build smoke.tex
%% =====================================================================

\documentclass{biomed-report}

\reporttitle{BioMed-QAgent}
\reportsubtitle{模板测试}
\reportshorttitle{BioMed-QAgent}
\reportteam{项目组}
\reportschool{北京理工大学}
\reporttrack{测试}
\reportdate{2026 年 8 月}

\begin{document}

\makereporttitle

\frontmatter

\chapter{模板测试}

正文测试。

\section{一级标题}

中文正文。English text. \(a^2+b^2=c^2\)。

\subsection{代码}

\begin{lstlisting}
const result = pipeline.run(input);

if (result.ok) {
    console.log(result);
}
\end{lstlisting}

\subsection{列表}

\begin{enumerate}
  \item 第一项。
  \item 第二项。
\end{enumerate}

\begin{keypoint}[核心要点]
这是 keypoint 测试。
\end{keypoint}

\begin{resultbox}[关键结果]
这是 resultbox 测试。
\end{resultbox}

\end{document}
