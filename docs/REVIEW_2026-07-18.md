# BioMedQAgent 流程审查报告（2026-07-18）

> 本审查基于 2026-07-18 当日代码状态，由前后端完整启动 + 多轮 search agent 深度探索得出。
> 所有结论均给出文件路径与行号锚点，便于后续修复定位。
> 第一轮发现 3 个 P0 级问题，第二轮扩展为 10 类问题；本报告与 `docs/TODO.md` §8 双向同步。

---

## 0. 核心架构性缺陷（根因收敛）

**核心问题**：`TaskManager._execute` 的"成功证据校验"完全缺失。

`backend/app/runtime/manager.py:1201-1212`：

```python
execution.seal_completion()
completion_events = await execution.commit_completion()   # 可能返回 []
for completion_event in completion_events:
    await self._append_completion_status(accepted, completion_event.payload, ...)
await self._append_completion_status(accepted, RunCompletedPayload())  # 无条件发射
```

`RunCompletedPayload`（`backend/app/domain/contracts/events.py:179-181`）是**空 payload**，无 validation 字段、无 artifact 引用、无 final_output 内容、无 finish_reason。一旦 `run_executor` 返回无异常，manager **无条件**发射 `RunCompletedPayload`，不校验是否真的产出了任何东西。

这一缺陷同时导致：
- 第 1 节（LLM 完成后部分案例结果未显示）
- 第 2 节（LLM 截断却标记为 COMPLETED）

**单一最高杠杆修复点**：在 `manager.py:1201-1212` 之间，AGENT 模式下增加"成功证据校验"：至少 1 个 `artifact_produced` 事件 + final_output 非空。不达标转 `RunFailedPayload`。这一改动能同时解决两个核心症状。

---

## 1. LLM 完成后部分案例"结果"未显示（P0）

### 1.1 根因链路

1. Agent 模式下，`_load_artifact_payloads`（`backend/app/agent_loop/runner.py:104-133`）在两种情况下**静默返回 `[]`**：
   - `run_manifest.json` 不存在（LLM 没调 `run_research_pipeline` / pipeline 失败 / 被取消）
   - `manifest_fingerprint == previous_fingerprint`（同 task 二次 run，内容未变）
2. `if payloads:` 守卫（`runner.py:284-305`）导致 `set_completion_committer` 未被调用，`_completion_committer` 保持 `None`
3. `commit_completion()` 返回 `[]`，但 `manager.py:1209` 仍发射 `RunCompletedPayload`
4. 前端收到 `run_completed` 但从未收到 `artifact_produced`
5. HTTP `list_artifacts`（`backend/app/api/routes.py:538-604`）因为 `.runtime-publication.json` 标记不存在，**永远返回 `[]`**
6. 前端 `ResultsViewer.tsx:274-282` 显示"暂无结果"

### 1.2 次要假设

- **假设 B**：WS `run_manifest` artifact_produced 事件的 RESET 逻辑（`frontend/src/runtime/reducer.ts:890-912`）与 HTTP hydration 存在竞态，WS gap 补传失败时 CSV 永远不回填
- **假设 C**：`_emit_activity` cancellation 守卫（`manager.py:1340-1350`）在 cancel + completion 同时发生时丢弃 `ArtifactProducedPayload`

### 1.3 关键代码位置

| 主题 | 文件:行号 |
|---|---|
| `_load_artifact_payloads` 静默返回 [] | `backend/app/agent_loop/runner.py:104-133` |
| `if payloads:` 守卫 | `backend/app/agent_loop/runner.py:284-305` |
| `_write_artifact_publication_marker` | `backend/app/agent_loop/runner.py:136-156` |
| manager 无条件发射 RunCompletedPayload | `backend/app/runtime/manager.py:1209-1212` |
| `RunCompletedPayload` 空 payload | `backend/app/domain/contracts/events.py:179-181` |
| HTTP `_load_validated_manifest` | `backend/app/api/routes.py:538-604` |
| 前端 manifest RESET 竞态 | `frontend/src/runtime/reducer.ts:880-914` |
| 前端 `mergeTaskArtifacts` watermark | `frontend/src/stores/agentStore.ts:65-112` |
| 前端 ResultsViewer 空判定 | `frontend/src/components/ResultsViewer.tsx:248-283` |

### 1.4 修复方案

| 优先级 | 修复点 | 改动 |
|---|---|---|
| P0 | `runner.py:284` | `_load_artifact_payloads` 返回 `[]` 时发射 `warning` 事件（code=`artifact_manifest_missing` 或 `artifact_unchanged`），前端 reducer 已支持 warning 展示分支 |
| P0 | `manager.py:1209` | AGENT 模式下，若 `completion_events` 为空且无 cancellation，改发射 `RunFailedPayload`，错误信息"agent 完成但未产出 artifact" |
| P1 | `runner.py:115` | 指纹未变不应静默返回 `[]`，应仍发射 `artifact_produced`（让前端至少能 hydration），或显式标记 `artifact_unchanged` 让前端走 HTTP 拉取 |
| P1 | `routes.py:548-549` | `_load_validated_manifest` 在 `.runtime-publication.json` 缺失时回退到 manifest 文件本身（降级），响应中加 `degraded=true` |

---

## 2. LLM 截断但流水线状态变为"完成"（P0）

### 2.1 根因链路

1. `build_agent`（`backend/app/agent_loop/agent.py:112-117`）构造 Agent 时**未设置**：
   - `output_type`（对比 summarizer 有校验）
   - `input_guardrails` / `output_guardrails`
   - `max_turns`（对比 `compaction.py:239` summarizer 显式 `max_turns=1`）
2. `_build_delegate`（`backend/app/agent_loop/model.py:29-38`）不传 `max_tokens`，LLM 默认截断时 DashScope 返回 `finish_reason="length"`
3. `_consume_events` 完成判定只有 `except StopAsyncIteration: return`（`runner.py:189-190`），不读 `result.final_output`、不读 `finish_reason`
4. `_extract_text_delta`（`runner.py:440-459`）只读 `delta.content`，**完全忽略 `finish_reason`**
5. OpenAI Agents SDK 在 `finish_reason="length"` 时**不抛异常**，把 partial content 当 `final_output` 返回
6. manager 同第 1 节的"无条件 RunCompletedPayload"路径
7. INSTRUCTIONS 中"必须调用 run_research_pipeline"（`agent.py:36`）是**软约束**，Agent 完全可以只生成讨论文字就结束

### 2.2 5 条 silent-completed 路径

| 假设 | 触发条件 | 优先级 |
|---|---|---|
| H1 | LLM `max_tokens` 截断，SDK 不抛异常 | 最高 |
| H2 | LLM 不调 tool 直接生成讨论文字结束 | 高 |
| H3 | SDK 默认 `max_turns` 用尽（约 10） | 中 |
| H4 | `run_research_pipeline` 抛异常被 LLM 吸收 | 中 |
| H5 | Agent `final_output` 为 None，executor 不校验 | 低 |

### 2.3 关键代码位置

| 主题 | 文件:行号 |
|---|---|
| Agent 构造无 max_turns / output_type / guardrails | `backend/app/agent_loop/agent.py:112-117` |
| `_build_delegate` 无 max_tokens | `backend/app/agent_loop/model.py:29-38` |
| `Runner.run_streamed` 调用点 | `backend/app/agent_loop/runner.py:263-269` |
| stream 完成判定仅靠 StopAsyncIteration | `backend/app/agent_loop/runner.py:165-230` |
| `_extract_text_delta` 忽略 finish_reason | `backend/app/agent_loop/runner.py:440-459` |
| RunCompletedPayload 空对象 | `backend/app/domain/contracts/events.py:179-181` |
| 对比：summarizer 校验 final_output | `backend/app/agent_loop/summarizer.py:50-55` |

### 2.4 修复方案

| 优先级 | 修复点 | 改动 |
|---|---|---|
| P0 | `runner.py:440-459` | `_extract_text_delta` 增加 `finish_reason` 提取，在 `length` 时发射 `warning` 事件（code=`llm_output_truncated`） |
| P0 | `runner.py:271` | `_consume_events` 返回后，读取 `result.final_output`，若为 None 或空字符串则 raise `RuntimeError("agent returned empty final_output")`（参考 `summarizer.py:50-55`） |
| P0 | `manager.py:1209` | AGENT 模式下，若 `completion_events` 为空且无 cancellation，转 `RunFailedPayload` |
| P1 | `agent.py:112-117` | 增加 `max_turns`（**需谨慎评估，见第 11 节**）+ INSTRUCTIONS 加硬约束 |
| P1 | `model.py:35` | 显式设 `max_tokens`（如 8192）避免默认值过小，INSTRUCTIONS 要求 LLM 在被截断时输出 `[TRUNCATED]` 标记 |

---

## 3. 数据源可用性 & PubMed 数据条目（P0）

### 3.1 实情速查表

| 层 | 状态 |
|---|---|
| `/api/v1/databases` 暴露 | **7 个**：pubmed, geo, gdc, pdb, pubchem, reactome, xena（`routes.py:134-148` `load_database_skills`） |
| Pipeline 实际使用 | **2 个**：pubmed, geo（两道硬门控） |
| 硬门控 1 | `backend/app/pipeline/tool.py:30-31` `if set(normalized_databases) != {"pubmed", "geo"}: raise ValueError` |
| 硬门控 2 | `backend/app/domain/contracts/runtime.py:112-119` `validate_task_databases` fixture 模式强制 `{"pubmed", "geo"}` |
| Discovery live | `discovery.py:140-179` 只发 PubMed `efetch` + GEO `esearch/esummary` |
| Acquisition | `acquisition.py:42-51` `_resolve_geo_dataset` 只查 GEO；PubMed 在 Discovery 已进 `sources`，但 Acquisition **从不为 PubMed 产出 SourceAsset/DownloadAttempt** |
| Fixture | `backend/tests/fixtures/ncbi/gse178352/` 唯一 pinned case，**确实包含 PubMed PMID 34180400** |
| Database 枚举 | `enums.py:8-16` 只有 8 个值，**完全不含** EuropePMC/Unpaywall/UniProt/ChEMBL/OpenTargets/TCMSP/DrugBank/DisGeNET/CNKI/Wanfang |

### 3.2 PubMed 在最终产物中的分布

| 产物文件 | PubMed 行数 | 说明 |
|---|---|---|
| `literature.csv` | 1 | PMID/DOI/title/authors |
| `source_list.csv` | 1 | 通过 `sources` 遍历 |
| `source_relations.csv` | 1 | PubMed→GEO 关系 |
| `source_assets.csv` | **0** | 只有 GEO（无 PubMed SourceAsset） |
| `download_log.csv` | **0** | 只有 GEO 下载 |
| `main_data.csv` | **0** | GEO 表达谱 |
| `dataset_catalog.csv` | **0** | GEO |

**结论**：PubMed 在 fixture 中**存在**（PMID 34180400），但只以文献元数据形式进入 3 个 CSV，**不进入**"数据条目"类产物。这是设计选择，但观感上像"PubMed 没参与"。

### 3.3 修复方案

| 优先级 | 修复点 | 改动 |
|---|---|---|
| P0 | `tool.py:30-31` + `runtime.py:112-119` | 若要扩展 Pipeline 支持其他数据库，需先解除这两道硬门控；若维持现状，`/databases` 响应加 `pipeline_supported: bool` 字段标明哪些走 Pipeline、哪些是 Agent 直调 |
| P1 | `acquisition.py:113-207` | 为 PubMed 新增 acquisition 分支，复用 `discovery/pubmed.py` 中已有的 `download_supplementary` 工具，产出 PubMed 全文/附件 `SourceAsset`，使其进入 `source_assets.csv`/`download_log.csv` |
| P2 | `Database` 枚举 / 新增 skill 文件 | 用户提到的 EuropePMC/Unpaywall 等 10 个数据库需新增 skill 文件 + 枚举值 + fixture，**工作量很大**，建议先核对 `PROBLEM.md` 确认是必选还是规划中 |
| P2 | fixture 扩充 | 增加多个 pinned case（不同数据库组合），避免单 case 漏覆盖 |

---

## 4. 前端 Agent 动态可见性缺失（P0）

### 4.1 用户反馈

"前端需要看到更多 Agent 动态，例如工具调用（目前有但是更新少）、工具调用结果（找到了多少篇论文、多少调数据、清洗剩余多少条数据），现在能看到的消息太少。"

### 4.2 根因（三层叠加）

#### 4.2.1 后端：Pipeline stage 事件无数值字段

`backend/app/pipeline/runner.py:655-665, 689-695, 723-730` 发射的 `StageStartedPayload` / `ToolCalledPayload` / `StageCompletedPayload` **全部只携带 digest，无 N 篇/M 条/K 行等中间进度**。

#### 4.2.2 后端：Skills 的 `records_count` 仅写入 `RunContext.query_log`，无事件通道

`pubmed.py:129-134` 与 `geo.py:103` 调用 `run_ctx.log_query(query, source, "completed", records_count)`，但该日志只写入 `RunContext.query_log`（`agent_loop/context.py:229-240`），**不发射任何事件**。`acquisition.py:158-164` 的 `SourceAsset.size_bytes` 未转化为可读进度事件。`processing.py` 解析后 `ParsedDataset.row_count` 未发射。

#### 4.2.3 前端：Agent 模式下 stage 事件被显式丢弃

`frontend/src/runtime/reducer.ts:915-957`：

```typescript
case "stage_started":
case "stage_completed":
case "stage_failed":
case "stage_skipped":
    if (task.summary.mode !== "fixture") break;   // ← Agent 模式丢弃
    ...
```

**Agent 模式任务调用 `run_research_pipeline` 时，所有 stage 事件被前端丢弃**。即使后端补发 progress 事件也看不到。

#### 4.2.4 前端：Chat 主流只渲染 messages 数组

`frontend/src/components/ChatPanel.tsx:105, 474-501` 只读 `selectActiveMessages`。工具调用与输出**完全不在 Chat 中展示**，只在 `AgentProgress.tsx:13-27` 顶部条（单行"当前工具名"）+ 独立 `ToolTrace.tsx` 侧边 Sheet（默认折叠，需点击 `TerminalIcon` 按钮才打开）可见。`ResearchPipeline.tsx:98-133` 只画 5 个阶段卡片 + 状态 Badge，无任何数值。

### 4.3 关键代码位置

| 主题 | 文件:行号 |
|---|---|
| `_consume_events` 处理 SDK 事件 | `backend/app/agent_loop/runner.py:165-230` |
| Pipeline stage 事件发射 | `backend/app/pipeline/runner.py:655-730` |
| `pubmed.py` log_query 不发事件 | `backend/app/skills/builtin/discovery/pubmed.py:129-134` |
| `geo.py` log_query 不发事件 | `backend/app/skills/builtin/acquisition/geo.py:103` |
| `RunContext.log_query` | `backend/app/agent_loop/context.py:229-240` |
| `ToolCompletedPayload` 字段 | `backend/app/domain/contracts/events.py:101-113` |
| 前端 stage 事件被丢弃 | `frontend/src/runtime/reducer.ts:915-957` |
| `tool_started/completed` reducer | `frontend/src/runtime/reducer.ts:767-824` |
| ChatPanel 只读 messages | `frontend/src/components/ChatPanel.tsx:105, 474-501` |
| AgentProgress 单行 | `frontend/src/components/AgentProgress.tsx:13-27` |
| ToolTrace 侧边 Sheet | `frontend/src/components/ToolTrace.tsx:102-129` |

### 4.4 修复方案

| 优先级 | 修复点 | 改动 |
|---|---|---|
| P0 | `events.py` | 新增 `StageProgressPayload(stage, kind, current, total, detail: dict)`，挂到 `EventPayload` 联合 |
| P0 | `pubmed.py` / `geo.py` | `search_*_adapter` 在 `run_ctx.log_query` 之后，通过 `RunContext` 注入的 emit 回调发射 `StageProgressPayload(kind="discovered_records", current=len(records), total=result.total_count)` |
| P0 | `acquisition.py:158-164` | 在 `SourceAsset` 创建后发射 `kind="downloaded_bytes"` / `kind="downloaded_records"` |
| P0 | `processing.py` | 在 `process_geo_tximport_counts` 返回后发射 `kind="cleaned_rows", current=parsed.row_count` |
| P0 | `runner.py` | 在 `_emit_stage_event` 旁新增 `_emit_progress_event(stage_attempt_id, payload)` 走 `_publish_event` |
| P0 | `reducer.ts:915` | 删除 `if (task.summary.mode !== "fixture") break;` 守卫，或为 agent 模式新增 stage 投影（类似 `fixtureStages` 但跨模式） |
| P0 | `AgentProgress.tsx:40-86` | agent 模式下增加 stage/进度区段（目前 fixture 模式才有） |
| P1 | `ChatPanel.tsx:406` | 新增 `StageProgressList` 组件读取 `task.activitiesById` 中 `kind==="progress"` 的活动 |
| P1 | `ToolTrace.tsx` | 默认折叠状态需暴露工具 output 摘要到 Chat 主流（在 `assistant_delta` 之间穿插"工具卡片"） |

---

## 5. 会话压缩（Compaction）正确性（P0）

### 5.1 现状

`backend/app/runtime/compaction.py:261-370` `ConversationCompactor.prepare()`：
- 触发条件：`unsummarized` 字符数 > `COMPACTION_CHARACTER_THRESHOLD=60_000`（L27）或 run 数 > `RAW_RUNS_AFTER_COMPACTION=5`（L28）
- 压缩通过 `_summarize_with_model`（L216-244）调用子 Agent，`max_turns=1`（L239）
- 失败降级走 `_fallback`（L409-440）：保留最近 `COMPACTION_FAILURE_RUNS=20` 条 runs 并发射 `WarningPayload(code="compaction_failed")`

### 5.2 缺陷

- **`max_turns=1` 不足**：summarizer 在 L242-243 校验 `final_output` 非空，但 `max_turns=1` 意味着 LLM 一旦被截断（`finish_reason="length"`）就抛 `ValueError`，触发降级路径——这与 project_memory 硬约束"LLM 失败必须抛异常"一致，但**缺少 `finish_reason` 显式校验**
- **query log 全局记录缺失**：`summarizer.py` 中的 `build_compress_query_log_tool`（L66-80）只把摘要写回 `RunContext.query_log_summary`（L254-260），**没有任何 ReviewerAgent**（grep 确认零匹配），也没有跨任务全局查询日志。project_memory 硬约束"压缩前完整传递给 ReviewerAgent"完全未实现

### 5.3 关键代码位置

| 主题 | 文件:行号 |
|---|---|
| `ConversationCompactor.prepare` | `backend/app/runtime/compaction.py:261-370` |
| `_summarize_with_model` max_turns=1 | `backend/app/runtime/compaction.py:216-244` |
| `_fallback` 降级 | `backend/app/runtime/compaction.py:409-440` |
| `build_compress_query_log_tool` | `backend/app/agent_loop/summarizer.py:66-80` |
| `query_log_summary` 写回 | `backend/app/agent_loop/summarizer.py:254-260` |

### 5.4 修复方案

| 优先级 | 修复点 | 改动 |
|---|---|---|
| P0 | `compaction.py:216-244` | summarizer 显式校验 `finish_reason`，`length` 时抛异常而非降级 |
| P0 | 新增 `ReviewerAgent` | 实现 `project_memory` 硬约束：压缩前完整传递 query log 给 ReviewerAgent |
| P1 | `compaction.py:27-29` | 阈值移入 `config.py`，避免硬编码 |

---

## 6. Follow-up Loop 循环控制完全缺失（P0）

### 6.1 现状

代码库中**完全不存在** follow-up 搜索机制。grep `followup|follow_up|IterationDecision` 在 `backend/app/` 下零匹配（除 `ws_events.py` 的 `task_not_found` 错误码外）。

### 6.2 缺陷（与 project_memory 硬约束冲突）

- **无 `followup_search` status 标识符**：`RunContext.log_query`（`context.py:229-240`）只记录 `query/source/status/records_count`，各 skill 自由使用 "ok"/"failed"/"error"/"succeeded"/"completed" 等不一致状态，**无 `not_found` 标准状态**
- **无最大 3 轮循环控制**：Agent loop 完全依赖 LLM 自主决定是否再次调用工具，无硬性轮次上限
- **失败查询不重试未强制**：各 acquisition skill 的失败处理不一致（如 `gdc.py:223` vs `pdb.py:135`），无统一去重逻辑
- **`IterationDecisionAgent` 已被 project_memory 硬约束要求"完全移除"**——但当前实现是根本没有 IterationDecision 机制，既未实现也未明确文档化"为什么不实现"

### 6.3 修复方案

| 优先级 | 修复点 | 改动 |
|---|---|---|
| P0 | `events.py` + `context.py` | 定义 `QueryStatus` 枚举（`success` / `not_found` / `failed` / `skipped` 四态），所有 skill 统一使用 |
| P0 | `agent.py` INSTRUCTIONS | 新增 follow-up 策略：最多 3 轮，失败查询标记 `not_found` 不重试 |
| P0 | 新增 `tests/test_query_log_status_consistency.py` | 遍历所有 skill 的 query_log 输出，断言 status ∈ QueryStatus |
| P1 | `runner.py` | Agent loop 增加 turn counter，达到 3 轮 follow-up 后强制停止并标记 `max_followup_reached` |

---

## 7. PDF 下载多源 fallback 链完全未实现（P0）

### 7.1 现状

grep `unpaywall|Unpaywall|EPMC|fullTextXML|europepmc|EuropePMC` 在 `backend/app/` 下**零匹配**。

- `integrations/acquisition.py:30-49` 的 `_ALLOWED_HOSTS` 白名单**仅含 NCBI/GDC/RCSB/PubChem/Reactome/Xena**，无 Unpaywall（`api.unpaywall.org`）和 EPMC（`www.ebi.ac.uk/europepmc/`）
- `pipeline/stages/acquisition.py:167-207` 的 `_run_acquisition_live` 仅下载 GEO counts 文件，**无 PDF 下载逻辑**
- `pipeline/tool.py:30-31` 硬拒绝非 pubmed/geo 数据库

### 7.2 缺失（与 project_memory 硬约束冲突）

- Unpaywall DOI 查询客户端（5s 快失败）
- EPMC fullTextXML via PMCID 客户端
- fallback 链编排器
- DOI/PMCID 解析器

### 7.3 修复方案

| 优先级 | 修复点 | 改动 |
|---|---|---|
| P0 | 新增 `integrations/unpaywall.py` | DOI 查询客户端，5s timeout，返回 pdf_url |
| P0 | 新增 `integrations/europepmc.py` | PMCID → fullTextXML 客户端 |
| P0 | `acquisition.py` | 实现 fallback 链：`pdf_url` → Unpaywall（DOI）→ EPMC fullTextXML（PMCID） |
| P0 | `integrations/acquisition.py:30-49` | `_ALLOWED_HOSTS` 新增 `api.unpaywall.org` / `www.ebi.ac.uk` |
| P1 | 新增 `tests/test_pdf_fallback_chain.py` | 验证三级 fallback 链各分支 |

---

## 8. 并发与资源管理（P1）

### 8.1 现状

- `TaskManager` 并发模型：`asyncio.Semaphore(max_active_runs=4)`（`manager.py:533`）+ 4 个 `_worker` 协程（L1016-1027）+ `_RemovableRunQueue` 有界队列（L425-500）。每个 task 通过 `_task_locks[task_id]` 串行化（L536）
- **Playwright 实例无复用**：`crawler.py:206-225` 用 `with sync_playwright() as p:` 上下文管理，**每次调用新建 browser**，无跨任务复用，效率低但无泄漏。4 个并发 task 可同时启动 4 个 Chromium，内存峰值高
- 文件 IO 隔离：每个 task 独立目录 `tasks/<task_id>/`，跨 task 无冲突。同 task 内 `TaskLock`（`state.py:250+`）基于 OS 文件锁（msvcrt/fcntl）。`atomic_write_json`（`repository.py:42-59`）用同目录 tmp + `os.replace` 保证原子性
- `artifacts/` 并发写：`publish.lock`（`validation.py:116-121`）保护同 task 的发布；跨 task 因目录隔离无冲突

### 8.2 修复方案

| 优先级 | 修复点 | 改动 |
|---|---|---|
| P1 | `crawler.py:206-225` | 引入 BrowserPool（单例 Chromium + 多 context），4 并发 task 共享一个 browser，每个 task 用 `browser.new_context()` 隔离 |
| P2 | `crawler.py` | 监控并发 Chromium 实例数，超阈值时排队 |

---

## 9. 错误处理与可观测性（P1）

### 9.1 现状

- Pipeline 异常捕获：`runner.py:325-348` 顶层 try 捕获所有异常 → `_finalize_failed`。stage 级异常 → `_finalize_stage_failed`（L682-687）。worker 异常 → `_handle_worker_failure`（`manager.py:1029-1074`）写 `RunFailedPayload`
- **结构化日志缺失**：`main.py:25-28` 仅 `logging.basicConfig(level=INFO, format="%(asctime)s [%(name)s] %(levelname)s: %(message)s")`。无 JSON formatter、无文件 handler、无 rotation、无 request_id/task_id 关联

### 9.2 关键错误吞掉点

| 位置 | 行为 |
|---|---|
| `pipeline/state.py:228` | `load_stage_output` 任何异常返回 None → 触发 stage 重跑（可接受但无日志） |
| `pipeline/runner.py:617` | `_collect_stage_output_files` 异常时 `expected_files = None`（无日志） |
| `validation.py:229` | cleanup journal 写入失败仅 `_LOGGER.exception`（继续运行） |
| `runtime/manager.py:1418-1423` | compaction event 发布失败仅 log，不抛 |
| `api/ws_events.py:362, 375, 381` | WebSocket 错误静默吞掉 |

### 9.3 修复方案

| 优先级 | 修复点 | 改动 |
|---|---|---|
| P1 | `main.py:25-28` | 引入 structlog 或 python-json-logger，所有日志带 `task_id` / `run_id` / `stage` 上下文 |
| P1 | `state.py:228` / `runner.py:617` | 异常路径增加 WARN 日志 |
| P1 | `ws_events.py:362, 375, 381` | WebSocket 错误增加 WARN 日志，避免静默吞掉 |
| P2 | 新增 `docs/observability.md` | 文档化日志格式、metric 暴露点、tracing 策略 |

---

## 10. 测试覆盖盲点（P0）

### 10.1 现状

- **Agent 模式 e2e 测试空白**：无 `test_agent_run_*` 测试用真实 LLM 调用走完 AgentRunExecutor。`tests/agent_loop/test_execution.py` 全部用 `NoopCompactor` + Mock SDK，不验证 `_load_artifact_payloads` 与 `RunCompletedPayload` 的真实交互
- **LLM 截断回归测试缺失**：grep `finish_reason|truncat.*model` 仅 1 匹配（`test_compaction.py:495` 断言 `max_turns=1`），**无针对 LLM 截断场景的回归测试**
- **无证据完成回归测试缺失**：第 1 节 P0 问题"LLM 完成工作后部分案例结果未显示"无对应回归测试。`test_runner_no_silent_fallback_on_failure`（`resilience.py:389-413`）仅测 validation 失败，不测 Agent 模式空 artifact 完成
- **followup 循环回归**：无（功能不存在）
- **PDF fallback 回归**：无（功能不存在）
- **并发 Playwright 测试**：`test_skill_browser.py:404` 测同 filename 并发，但不测多 task 并发 Chromium 实例数
- **Live 测试门槛高**：`tests/live/` 下 5 个文件全部需 `RUN_NCBI_LIVE=1`，默认跳过，CI 不覆盖
- **`test_qwen_task_spec_live.py`** 是唯一真实调用 Qwen 的测试，但绕过 Agent SDK，直接用 `AsyncOpenAI`，不验证 AgentRunExecutor 链路

### 10.2 修复方案

| 优先级 | 修复点 | 改动 |
|---|---|---|
| P0 | 新增 `tests/agent_loop/test_agent_run_e2e.py` | 用 Mock LLM 但走完 AgentRunExecutor 完整链路，验证 artifact_produced + RunCompletedPayload 配对 |
| P0 | 新增 `tests/agent_loop/test_silent_completion.py` | 回归第 1、2 节问题：LLM 不调 tool / final_output 为空 / manifest 缺失时必须 RunFailed |
| P0 | 新增 `tests/agent_loop/test_llm_truncation.py` | 模拟 `finish_reason="length"`，验证 warning 事件 + 不静默 completed |
| P1 | 新增 `tests/agent_loop/test_followup_loop.py` | followup 3 轮上限回归 |
| P1 | 新增 `tests/test_pdf_fallback_chain.py` | 三级 fallback 链各分支 |

---

## 11. Agent max_turns 谨慎设置（P1，需评估）

### 11.1 现状

`backend/app/agent_loop/agent.py:112-117` 构造 Agent 时**未设置 `max_turns`**，使用 OpenAI Agents SDK 默认值（约 10）。这导致两个问题：
- LLM 可能无限调用工具循环（虽未观察到，但理论上可能）
- LLM 可能干到一半被默认 max_turns 用尽截断，但 SDK 不抛异常，走 silent completed 路径（见第 2 节 H3）

### 11.2 谨慎设置原则

**不能简单设置一个较小值（如 5）了事**，因为：
- 复杂研究任务可能需要多轮工具调用（discovery + acquisition + processing + validation 各 1 轮 = 4 轮，加上 followup 可能 6-8 轮）
- 设置过小会导致正常任务被截断
- 设置过大失去保护意义

**建议方案**：
1. **分层设置**：主 Agent `max_turns=15`（覆盖正常 4-8 轮 + followup 3 轮 + 余量），summarizer 保持 `max_turns=1`
2. **达到 max_turns 后的处理**（**用户明确要求**）：
   - **不直接转 FAILED**，而是发射 `UserInputRequiredPayload(prompt_kind="max_turns_reached")` 暂停 Run，进入 `AWAITING_USER_INPUT` 状态
   - **前端展示"继续工作"按钮**：用户点击后调用 `POST /api/v1/tasks/{task_id}/runs/{run_id}/resume`，Agent 从当前会话状态继续执行下一轮
   - **用户也可选择"停止"**：转 CANCELLED
   - **复用 §4.2.1 已完成的 pause-resume 底层架构**（`_await_user_input` 原语），不另建机制
3. **同时实现第 2 节的 finish_reason 校验**：max_turns 用尽与 LLM 截断是不同场景，但都需可观测

### 11.3 关键代码位置

| 主题 | 文件:行号 |
|---|---|
| Agent 构造无 max_turns | `backend/app/agent_loop/agent.py:112-117` |
| 对比 summarizer max_turns=1 | `backend/app/runtime/compaction.py:239` |
| `_await_user_input` 暂停原语 | `backend/app/pipeline/runner.py`（见 §4.2.1） |
| `UserInputRequiredPayload` | `backend/app/domain/contracts/events.py` |
| `POST /resume` API | `backend/app/api/routes.py` |
| 前端 `UserInputDialog` | `frontend/src/components/UserInputDialog.tsx` |

### 11.4 修复方案

| 优先级 | 修复点 | 改动 |
|---|---|---|
| P1 | `agent.py:112-117` | 设置 `max_turns=15`（覆盖正常 + followup + 余量） |
| P1 | `runner.py:271` | `_consume_events` 后检测 SDK result 的 `max_turns_reached` 标志（若 SDK 暴露），发射 `UserInputRequiredPayload(prompt_kind="max_turns_reached")` 走 `_await_user_input` |
| P1 | `events.py` | `PromptKind` 联合新增 `"max_turns_reached"` |
| P1 | `UserInputDialog.tsx` | 新增 `prompt_kind="max_turns_reached"` 渲染分支，展示"Agent 已达到最大轮次，是否继续？" + [继续] / [停止] 按钮 |
| P2 | `agent.py` INSTRUCTIONS | 新增"达到 max_turns 后应输出 `[MAX_TURNS_REACHED]` 标记"指导 |

---

## 12. 配置硬编码（P2）

### 12.1 现状

`backend/app/config.py` 仅 16 个配置项。**硬编码值**：
- `crawler.py:35-39`：Chrome 131 UA 字符串硬编码
- `crawler.py:58`：`_RATE_LIMIT_SECONDS=2.0` 硬编码
- `compaction.py:27-29`：`COMPACTION_CHARACTER_THRESHOLD=60_000` / `RAW_RUNS_AFTER_COMPACTION=5` / `COMPACTION_FAILURE_RUNS=20` 硬编码
- `runner.py:81-89`：`DEFAULT_STAGE_TIMEOUTS` / `TOTAL_TIMEOUT=300.0` / `USER_INPUT_TIMEOUT=300.0` 硬编码
- `acquisition.py:34`：`_MAX_BYTES=100MB` 硬编码

### 12.2 降级合理性

- `DASHSCOPE_API_KEY` 缺失：启动时不报错，首次模型调用时抛 `ModelConfigurationError`（`model.py:23-26`）——延迟失败可能掩盖配置问题
- `NCBI_EMAIL/NCBI_TOOL` 默认占位值 `biomed-qagent@example.com`——NCBI 可能拒询
- `NCBI_API_KEY` 默认空——无 rate limit 提升
- `OUTPUT_DIR` 默认 `data/output`（相对路径）——cwd 依赖，生产环境风险

### 12.3 修复方案

| 优先级 | 修复点 | 改动 |
|---|---|---|
| P2 | `config.py` | 扩展配置项：`crawler_ua` / `crawler_rate_limit_seconds` / `compaction_*` / `stage_timeouts` / `max_download_bytes` |
| P2 | `config.py` | 启动时校验 `DASHSCOPE_API_KEY` 非空，否则 fail fast |
| P2 | `config.py` | `OUTPUT_DIR` 改为绝对路径默认值 |

---

## 13. 修复优先级总览

| 优先级 | 问题 | 核心修复点 |
|---|---|---|
| P0 | 第 1 节 + 第 2 节（同源） | `manager.py:1209` 增加成功证据校验 |
| P0 | 第 1 节 | `runner.py:284` 空 payloads 发 warning |
| P0 | 第 2 节 | `runner.py:440-459` 读 finish_reason + `runner.py:271` 校验 final_output |
| P0 | 第 3 节 | `tool.py:30` + `runtime.py:112` 解除硬门控或加 `pipeline_supported` 标记 |
| P0 | 第 4 节 | 新增 `StageProgressPayload` + 前端删除 `if (task.summary.mode !== "fixture") break;` |
| P0 | 第 5 节 | summarizer 校验 finish_reason + 实现 ReviewerAgent |
| P0 | 第 6 节 | 实现 QueryStatus 枚举 + followup 3 轮上限 |
| P0 | 第 7 节 | 实现 PDF 三级 fallback 链 |
| P0 | 第 10 节 | 补 Agent 模式 e2e + silent completion + LLM 截断回归测试 |
| P1 | 第 1 节 | `runner.py:115` 指纹未变不静默返回 [] |
| P1 | 第 2 节 | `agent.py:112-117` 加 `max_turns`（**谨慎，见第 11 节**） |
| P1 | 第 3 节 | `acquisition.py` 为 PubMed 新增 SourceAsset 产出 |
| P1 | 第 4 节 | ChatPanel 新增 `StageProgressList` + ToolTrace 暴露摘要 |
| P1 | 第 8 节 | `crawler.py` 引入 BrowserPool |
| P1 | 第 9 节 | 引入 structlog + 修复错误吞掉点 |
| P1 | 第 11 节 | max_turns 用尽后走 `_await_user_input` + 前端"继续工作"按钮 |
| P2 | 第 3 节 | 新增 EuropePMC/Unpaywall 等 skill（需核对 PROBLEM.md） |
| P2 | 第 9 节 | 文档化可观测性策略 |
| P2 | 第 12 节 | 配置项扩展 + 启动 fail fast |

---

## 14. 建议执行节奏

按 AGENTS.md §7.2 "一个功能,一个 merge"原则，建议分 4 批 merge：

### 批次 1：核心真实性修复（P0 同源问题）
- 第 1 节 + 第 2 节 + 第 10 节回归测试
- 单一最高杠杆：`manager.py:1209` 成功证据校验
- 配套：`runner.py` finish_reason 校验 + final_output 校验 + 空 payloads warning
- 配套：3 个回归测试文件
- 分支：`fix/silent-completion`

### 批次 2：前端 Agent 动态可见性（P0）
- 第 4 节
- 新增 `StageProgressPayload` + Skills 发射 progress + 前端删除 stage 事件丢弃守卫 + ChatPanel 工具卡片
- 分支：`feat/agent-visibility`

### 批次 3：max_turns + 用户继续按钮（P1）
- 第 11 节
- Agent 加 `max_turns=15` + max_turns 用尽走 `_await_user_input` + 前端"继续工作"按钮
- 分支：`feat/max-turns-continue`
- **依赖**：批次 1 的 final_output 校验

### 批次 4：Follow-up + PDF + Compaction（P0，较大）
- 第 5、6、7 节
- 分支：`feat/followup-pdf-compaction`
- **依赖**：批次 1 的 QueryStatus 枚举

---

## 15. 多课题 live 测试额外发现并修复的问题（2026-07-18）

> 批次 1-3 合并后，对 3 个课题（阿尔茨海默病/TP53/Huntington's）进行 live 测试，
> 发现并修复了 8 个数据流与 LLM 鲁棒性问题。分支
> `fix/stage-progress-and-llm-json` → merge commit `9aed497`。

### 15.1 前端 stage_progress 事件被静默丢弃

- **现象**：批次 2 已发射 `StageProgressPayload`，但前端"已发现记录""已下载字节"等
  数字始终不显示。
- **根因**：`frontend/src/runtime/transport.ts` 的 `EVENT_TYPES` 白名单 Set
  遗漏 `"stage_progress"`，事件在 transport 层被丢弃，reducer 永远收不到。
- **修复**：`EVENT_TYPES` 添加 `"stage_progress"`。

### 15.2 中文课题在 PubMed 零结果

- **现象**：`stage_failed` — "PubMed search returned no records for topic:
  阿尔茨海默病与骨质疏松症共病机制研究"。
- **根因**：Pipeline discovery 阶段用原始中文 topic 搜索 PubMed，PubMed 对中文
  查询返回 0 结果。Agent 之前已通过英文查询找到 PMID/GSE，但没传给 Pipeline。
- **修复**：`run_research_pipeline` 工具新增 `pmid`/`gse` 可选参数；
  `_build_tool_specification` 构建显式 accession 的 `TaskSpecification`
  （`{pmid}[PMID]`、`{gse}[Accession]`），用直接 NCBI 查询替代主题搜索；
  Agent system prompt 指示传递已发现的 accession。

### 15.3 GEO tximportCounts.txt.gz 404

- **现象**：`stage_failed` — "live download failed: download returned HTTP 404"。
- **根因**：`GSE260461` 没有 `tximportCounts.txt.gz`（并非所有 GEO series 都有
  此文件）。
- **修复**：`_run_acquisition_live` 添加候选 URL 列表，先试 tximport counts URL，
  404 后回退到 universally-available 的 `series_matrix.txt.gz`。
  `_try_acquire` 辅助函数返回 `None` 而非抛异常。

### 15.4 Processing 无法解析非 tximport 格式

- **现象**：`process_geo_tximport_counts` 严格要求 tximport counts 格式
  （12 个 counts 列），series_matrix 格式会抛 `ValueError`。
- **修复**：`run_processing` 用 try/except 包裹 tximport 解析，失败时调用
  `_build_minimal_parsed_dataset` 生成仅含表头的 0 行 CSV，使 pipeline 仍能产出
  有效 artifact 包（literature、dataset_catalog、source_list）。

### 15.5 validation source_asset_integrity 路径计算 bug

- **根因**：`validation.py` 中 `relative_to` 路径计算硬编码了 task_dir 层级，
  与实际 workdir 结构不匹配。
- **修复**：动态查找 `source_assets` 组件确定 task_dir。

### 15.6 discovery 阶段 live 模式硬编码 PMID/GSE

- **根因**：`_DEFAULT_PMID`/`_DEFAULT_GSE` 在 live 模式下仍被用作 fallback，
  导致非 GSE178352 课题失败。
- **修复**：live 模式改为按课题搜索 NCBI；添加 `logger.info` 诊断日志
  输出 resolved pmid/gse、specification 来源、discovery 成功结果。

### 15.7 Qwen 偶发 400 (function.arguments 非 JSON)

- **现象**：`run_failed` — `Error code: 400 - 'function.arguments' parameter
  of the code model must be in JSON format`。
- **根因**：Qwen/DashScope 偶发返回非 JSON 的 `function.arguments`，下次请求
  DashScope 返回 400。Huntington's 课题首次测试因此失败。
- **修复**：`AgentRunExecutor` 新增 `_is_qwen_function_args_error` 检测函数
  和 `QWEN_FUNCTION_ARGS_RETRY_LIMIT=2`；捕获该错误后用原始 `execution.input`
  重跑 Run（避免把 malformed tool call 带入下一轮 conversation history 再次
  触发 400），每次重试发射 `WarningPayload(code="llm_function_args_retry")`。
- **回归测试**：`tests/agent_loop/test_qwen_function_args_retry.py` 3 个测试
  覆盖"首次 400 → 重试成功"、"重试次数用尽 → RunFailed"、错误检测函数。

### 15.8 frontend reducer.ts TypeScript 类型收窄

- **现象**：`pnpm build` 报 `TS2339: Property 'attempt' does not exist on
  type 'never'`。
- **根因**：`existing ?? {...}` 右分支里 `existing` 被 TS 收窄为 `never`，
  导致 `existing?.attempt` 等访问报错。
- **修复**：改为显式 `if/else` 避免 TS 类型收窄陷阱。

### 15.9 多课题 live 测试结果

| 课题 | task_id | 状态 | artifacts |
|------|---------|------|-----------|
| 阿尔茨海默病与骨质疏松症共病机制研究 | task_30c319c1 | completed | 14 |
| TP53 tumor suppressor gene mutation mechanism in breast cancer | task_795386d0 | completed | 14 |
| Huntington's disease pathogenesis and therapeutic targets | task_f1e9fe05 | completed | 14 |

### 15.10 质量门禁

- backend: 895 passed, 1 skipped, 18 deselected, ruff 0 errors
- frontend: 195 passed, lint 0 errors, tsc 0 errors, build success
- 3 个新回归测试（Qwen 重试）

---

## 16. LLM 编造 artifact 文件名/列名（2026-07-18）

> 用户反馈："LLM 报告有主数据 csv（merged_comorbidity_data.csv）但我未在本地找到，
> 前端的'结果'页也没有找到这个 CSV"。分支 `fix/llm-artifact-naming`。

### 16.1 现象

- 用户测试"阿尔茨海默病与骨质疏松症共病机制"课题，LLM 最终报告写：
  > **主数据 CSV (`merged_comorbidity_data.csv`)**：
  > 表格列包括：`Gene_Symbol`、`Pathway_Name`、`Role_in_AD`、
  > `Role_in_Osteoporosis`、`Evidence_Level`、`Source_Database`、`Accession_ID`
- 用户在 `artifacts/` 目录和前端"结果"页都找不到 `merged_comorbidity_data.csv`
- 实际 Pipeline 产出的是 `main_data.csv`（14 个固定文件名之一）
- 实际 `main_data.csv` 的列是 `record_id`/`dataset_id`/`gene_id` 等 22 列，
  不是 LLM 编造的 `Gene_Symbol`/`Pathway_Name` 等

### 16.2 根因（两层叠加）

1. **`pipeline/tool.py` 返回值缺少 artifact 文件名清单**
   - `run_research_pipeline` 工具返回值只有 `artifact_count: 14`，
     没有 `artifacts: [{name, size_bytes, media_type}, ...]`
   - LLM 看不到实际产出了哪些文件，只能根据课题主题编造

2. **`agent.py` system prompt 没有告知固定文件名/列名**
   - system prompt 只说"主数据 CSV — 合并、清洗后的表格数据"
   - 没有告知 Pipeline 产出的 13 个固定 CSV 文件名
   - 没有告知 `main_data.csv` 的 22 个固定列名
   - 没有明确禁止 LLM 编造文件名/列名

### 16.3 修复

1. **`pipeline/tool.py` 返回值新增 `artifacts` 字段**
   ```python
   "artifacts": [
       {"name": entry.name, "size_bytes": entry.size_bytes, "media_type": entry.media_type}
       for entry in manifest.artifacts
   ],
   "note": "Artifacts are published ... with the exact names listed above. "
           "Reference these filenames verbatim ... do not invent filenames."
   ```

2. **`agent.py` system prompt 新增两个固定清单小节**
   - "Pipeline 产出的固定 artifact 文件名（禁止编造）"：13 个 CSV + run_manifest.json 表格
   - "`main_data.csv` 的固定列名（禁止编造）"：22 个列名（`_OUTPUT_COLUMNS`）

3. **回归测试**：`test_pipeline_tool.py::test_pipeline_function_tool_runs_explicit_fixture_mode`
   扩展断言：返回值包含 `artifacts` 列表、`main_data.csv` 在其中、
   每个 entry 含 `name/size_bytes/media_type`、`note` 含 "do not invent filenames"。

### 16.4 验证

- backend: 6 个 pipeline_tool 测试全通过，58 个 agent_loop 测试全通过
- ruff: 我改的 3 个文件 0 errors
- 预先存在的 ruff 错误（stats.py E402、conftest.py SIM105）和 flaky 测试
  （test_skill_pdb_success）与本次改动无关，未触碰

---

## 17. 全仓库代码审查优化（2026-07-18，karpathy-guidelines）

> 用户指令：`/karpathy-guidelines 进行全仓库的代码检查优化`，附加任务：
> 流程阻塞评估移除、逻辑 bug 修复、假实现/假接入记录留待后续报告。
> 分支：`fix/code-review-surgical`。

### 17.1 外科手术式修复（已合并到本分支）

| # | 文件 | 问题 | 修复 |
|---|------|------|------|
| 1 | `frontend/src/components/AgentProgress.tsx:126-130` | BLOCKER：`task.runsById[task.runOrder.length - 1]` 用数字索引当 key，应为字符串 run_id | 改为 `lastRunId = task.runOrder[task.runOrder.length - 1]` 再查 `runsById[lastRunId]` |
| 2 | `frontend/src/runtime/transport.ts:281-290` | BUG：`ping()` 无超时，服务端不回 pong 时 controlBarrierQueue 永久阻塞 | 新增 `pingTimeoutMs`（默认 10s），`PendingPing.timer` 在 resolve/reject 时清理 |
| 3 | `backend/app/skills/builtin/analysis/stats.py:535-541` | BUG：`round(float(np.nan), 4)` 产生 `NaN`，`json.dumps` 默认 `allow_nan=True` 输出非法 JSON，前端 `JSON.parse` 崩 | 新增 `_safe_float()` 辅助，NaN/Inf/None → None（JSON null） |
| 4 | `backend/app/skills/builtin/processing/extract_tables.py:571-572` | BUG：`match.group(0)` 返回整行（含 "Figure 1:" 前缀），应为 `group(2)`（caption 正文） | 改为 `match.group(2).strip()`，group(2) 为空时回退 group(0) |
| 5 | `backend/app/skills/builtin/discovery/understanding.py:112-157` | BUG：`db_names` 含 "Xena" 但 `_ACCESSION_PATTERNS_BY_DB` 无对应条目，Xena 检测永远空 accessions | 新增 `"Xena": re.compile(r"\bhttps?://\S*?xena\S*?\.(?:net|org)\b", re.IGNORECASE)` 匹配 hub URL |
| 6 | `backend/app/skills/builtin/acquisition/reactome.py:240-253` | BUG：`get_pathway` 的 `name`/`summation` 未调用 `_strip_html`，与 search 路径（line 162/169）不一致，HTML 高亮标签泄漏到用户报告 | `name` 和 `summation` 都过 `_strip_html()` |
| 7 | `backend/app/skills/builtin/acquisition/gdc.py:494-495` | BUG：`run_ctx.add_raw_asset(local_files[0])` 只注册 manifest JSON，实际下载的 TSV 数据文件未注册 | 改为 `for data_file in local_files[1:]: run_ctx.add_raw_asset(data_file)`（manifest 是元数据，不是 raw 数据） |
| 8 | `backend/app/skills/builtin/acquisition/pdb.py:40-78,181` | BUG（AGENTS.md 硬约束违反）：`_post_json`/`_get_json`/`_download` 无 rate limiting，仅 enrichment 循环内有 `time.sleep` | 新增模块级 `_rate_limit()`（2s/req，与 gdc.py 一致），三个函数入口调用；移除冗余的 `time.sleep(_RATE_LIMIT_SECONDS)` |
| 9 | `backend/app/skills/builtin/acquisition/xena.py` 全文 | BUG（AGENTS.md 硬约束违反）：无 rate limiting；`_USER_AGENT = "BioMed-QAgent/0.1 (...)"` 非浏览器 UA | 新增 `_rate_limit()`（2s/req）；UA 改为 Chrome 120 字符串；`_fetch_hub_index` 和 `_download` 入口调用 `_rate_limit()` |
| 10 | `backend/tests/conftest.py` | 测试基础设施：autouse fixture 只禁用 `app.tools.crawler._rate_limiter.wait`，未覆盖 pdb/xena 新增的 `_rate_limit()`，测试会真实 sleep 2s | 扩展 autouse fixture，patch gdc/pdb/xena 三个模块的 `_rate_limit` |
| 11 | `backend/tests/test_skill_pdb.py:93-120` | 测试：`assert _sleep.call_count == 2` 断言旧的 `time.sleep` 调用次数，rate limiting 重构后失效 | 移除 `_sleep` patch 和断言；rate limiting 行为由 conftest fixture 覆盖 |

### 17.2 评估后未修复（非阻塞 / 需更大重构）

| 项 | 文件 | 原因 |
|---|------|------|
| `agentStore.ts:140` `new Date(0).toISOString()` | `frontend/src/stores/agentStore.ts` | 初判为 BUG（1970 时间戳导致排序错乱），实际是**有意设计**：本地新建任务在服务端确认 `created_at` 前排到列表末尾，避免历史任务加载时顺序跳动。`runtime-controller.test.ts:353` 验证此行为。已回滚。 |
| `useAPI.ts:140-147` postAdmission 无条件重试 | `frontend/src/hooks/useAPI.ts` | 初判为 BUG（POST 重试可能创建重复任务），实际 `APIError` 由 `parseResponse` 在 try 外抛出，catch 只捕获 fetcher 层网络错误；且 `request_id` 提供幂等性。无需修复。 |
| `cleaning.py:75-78` int/float 双向兼容 | `backend/app/tools/cleaning.py` | SMELL：`expected=int, actual=float` 静默接受（"1.5" 当 int 不报错）。但注释明确写"int 和 float 兼容"，改动可能破坏下游依赖此宽松性的消费者。留待后续评估。 |
| `gdc.py:38-48` `_last_request_ts` 无锁竞态 | `backend/app/skills/builtin/acquisition/gdc.py` | 理论竞态：多线程并发调用 `_rate_limit()` 可能读写竞态。实际 agent loop 单线程顺序调用，不会触发。不修复。 |
| `evolution.py` 路径穿越 + 任意代码写入 + RCE | `backend/app/skills/evolution.py:55-59,198` | BLOCKER 安全漏洞，但修复需 name 正则白名单 + code AST 白名单（较大重构）。见 §17.3。 |

### 17.3 假实现 / 假接入清单（留待后续报告）

| # | 文件:行 | 假实现描述 | 影响 |
|---|---------|-----------|------|
| 1 | `backend/app/skills/builtin/acquisition/geo.py:139-169` | `describe_geo_adapter` 的 `payload.update({...})` 硬编码 `overall_design=""`、`platforms` 的 `title=""`/`organism=""`、`supplementary_file_urls=[]`。NCBI E-utilities 的 `esummary` 实际不返回这些字段，但代码结构暗示已填充。 | 用户报告里 GEO 数据集的 design/platform/补充文件信息永远为空。需接 GEO 自有的 `esummary` 或 `info` 端点补全。 |
| 2 | `backend/app/skills/builtin/acquisition/pubchem.py:134,215` | page fallback 路径 `run_ctx.log_query(term, "pubchem", "ok", 1)` 报"成功 1 条"，但 `_page_fallback` 只返回 `body_text_preview`（HTML 可见文本截断），无结构化记录。 | query_log 的"成功"计数虚高，metrics 误导。应记 `status="page_fallback"` 或 `count=0`。 |
| 3 | `backend/app/skills/builtin/acquisition/reactome.py:284` | 同 pubchem 模式：page fallback 前 `log_query(pathway_id, "reactome", "ok", 1)`，但 `_page_fallback` 只返回预览文本。 | 同上。 |
| 4 | `backend/app/skills/evolution.py:103-139` | `create_evolution_md` 模板含 4 处 `[to be filled]` 占位符（Browser Steps / Downloaded Files / Verification / Known Limitations）。`save_learned_skill` 写入磁盘后从不回填。 | EVOLUTION.md 永远是模板状态，无实际演化追踪价值。 |
| 5 | `backend/app/tools/crawl_signal.py:21-109` | 整个模块 `requires_crawl()` 函数从未被任何 skill 调用（pubchem/reactome 直接 inline `fetch_with_fallback`，不走此信号）。模块文档描述的"三层 fallback 信号链"未实现。 | 死代码。模块要么删除，要么真正接入 skill 的 fallback 路径。 |
| 6 | `backend/app/skills/evolution.py:55-59` + `self_evolution.py:22-80` | **BLOCKER 安全漏洞（假接入的真实危险）**：`save_workflow_as_skill` 是 `@function_tool`，LLM 可调用并传入任意 `name` 和 `code`。`save_learned_skill` 无 name 校验（路径穿越：`name="../../malicious"` 可写到 learned/ 之外）、无 code 校验（任意 Python 代码写入 .py）。`load_learned_skill` 用 `importlib.import_module` 执行该代码，完成 RCE 链。 | prompt 注入或恶意用户可借此执行任意代码。**修复方向**：(a) name 正则白名单 `^[a-z][a-z0-9_]*$`；(b) code AST 分析拒绝 `import`/`exec`/`eval`/`open`/`subprocess`/`os.system` 等；(c) 在修复完成前禁用 `self_evolution` skill 注册。 |

### 17.4 质量门禁

- **backend**: `uv run ruff check app/ tests/ launcher.py` → 0 errors；`uv run pytest` → 896 passed, 18 deselected
- **frontend**: `pnpm lint` → 0 errors；`pnpm tsc` → 0 errors；`pnpm test` → 200 passed；`pnpm build` → 成功
- **uvicorn 启动**: `uv run uvicorn app.main:app` → Application startup complete

---

## 18. main_data.csv 0 行 + sample_metadata.csv 全空修复（2026-07-18）

### 18.1 现象

用户对 snRNAseq / RNA-seq 课题（如 GSE260461）执行 live 任务后，`artifacts/main_data.csv`
**只有第一行表头，0 行数据**，且 `artifacts/sample_metadata.csv` 也完全为空（仅表头）。
LLM 报告中对样本元数据的描述全部失真。

### 18.2 根因

GEO 现代 series（snRNAseq / RNA-seq / 高通量测序）的 `*_series_matrix.txt.gz`
文件**只含样本元数据行（`!Sample_*`），表达矩阵 block 为空**——`!series_matrix_table_begin`
和 `!series_matrix_table_end` 之间只有表头行。

Pipeline 的处理链路：

1. `process_geo_tximport_counts` 严格要求 GSE178352 风格的 12 列 `counts.*`
   表头，遇到 series_matrix 直接抛 `ValueError`。
2. 上一轮添加的 `_build_minimal_parsed_dataset` 回退生成 0 行 CSV（仅表头），
   但 `samples` 列表保持空——因为 `parse_geo_soft_samples` 只识别
   GSE178352 的 SOFT 格式（`^SAMPLE = ` + `!Sample_description = Sample ...`）。
3. `artifact_build` 用 `samples=[]` 写 `sample_metadata.csv`，导致该文件也为空。
4. `GeoSampleMetadata.source_alias` pattern `^[AB]\d+$` 是 GSE178352 专有的
   A/B 命名约定，无法接受 GSM accession 作为 alias。

### 18.3 修复

**分支**：`fix/series-matrix-empty-data`

1. `backend/app/pipeline/processing/geo_tximport.py`
   - 放宽 `GeoSampleMetadata.source_alias` pattern：`^[AB]\d+$` → `^[A-Za-z0-9_-]+$`，
     允许 GSM accession 作为 alias。
   - 新增 `parse_geo_series_matrix_samples(compressed: bytes)`：从
     `!Sample_geo_accession` / `!Sample_title` / `!Sample_organism_ch1` /
     `!Sample_characteristics_ch1` 行恢复 `GeoSampleMetadata` 列表；
     `source_alias` 回退为 GSM 本身；`treatment` 回退为 title；
     `replicate` 默认 1；`cell line` characteristic 走 `_CELL_LINE_CANONICAL` 修正。
   - 新增辅助函数 `_split_series_matrix_row` 处理 tab 分隔 + 双引号包裹的值。

2. `backend/app/pipeline/stages/processing.py`
   - `run_processing` 的 except 分支新增 `_recover_samples_from_series_matrix`
     调用：先尝试 series_matrix 样本恢复，再生成 minimal ParsedDataset。
   - 新增 `_recover_samples_from_series_matrix`：读取 `source_asset` 文件字节，
     调用 `parse_geo_series_matrix_samples`；解析失败返回空列表并记 warning。
   - 修复后 `sample_metadata.csv` 即使在 `main_data.csv` 0 行时也有真实样本数据。

3. `backend/app/agent_loop/agent.py`
   - system prompt 新增"main_data.csv 为 0 行时的处理（重要）"小节：
     明确告知 LLM 当 series_matrix 表达矩阵为空时，`main_data.csv` 0 行是
     正常的，必须如实说明，**不得编造表达值/基因名/路径**，应展示
     `sample_metadata.csv` 的样本数和关键字段，并建议用户从 GEO supplementary
     files 下载 Seurat/HDF5 对象以获取表达矩阵。

4. `backend/tests/pipeline/test_geo_tximport_processing.py`
   - `test_series_matrix_parser_recovers_samples_from_empty_matrix_block`：空 matrix
     block 仍能恢复 3 个样本，cell_line/treatment/replicate 全部正确解析。
   - `test_series_matrix_parser_treatment_falls_back_to_title`：无 treatment
     characteristic 时回退到 title。
   - `test_series_matrix_parser_raises_on_missing_accession_row`：缺 accession 行
     抛 `ValueError`。
   - `test_geo_sample_metadata_accepts_gsm_accession_as_source_alias`：放宽后的
     pattern 接受 `GSM9000001` 作为 source_alias。

### 18.4 验证

- 6 个 series_matrix parser 测试全部通过
- backend `uv run pytest` → 896 passed, 18 deselected
- backend `uv run ruff check` → 0 errors

### 18.5 后续建议

- `main_data.csv` 0 行对终端用户仍不友好。未来可考虑：
  - 从 GEO supplementary files 下载 `GSEnnnnn_RAW.tar` 并解析其中的
    单样本 count 矩阵；
  - 对 snRNAseq series，调用 GEO 的 `supplementary_file` URL 提取
    Seurat/HDF5 对象的元数据（cell barcodes + clusters）；
  - 在 `quality_report.csv` 中显式标记 `main_data_rows=0` 的 reason code。
- LLM 报告层面，可考虑在 `run_research_pipeline` 返回值中增加
  `main_data_row_count` 字段，让 LLM 在 system prompt 之外也通过结构化数据
  感知"0 行"状态。

---

## 19. main_data.csv 始终含样本元数据行（2026-07-18）

### 19.1 现象

§18 修复了 `sample_metadata.csv` 全空的问题，但 `main_data.csv` 在
snRNAseq / RNA-seq series（series_matrix 表达矩阵 block 为空）下**仍然只有
表头 0 行数据**。用户反馈："用户只要查看 main_data.csv 即可获得所有信息"，
而当前设计下用户必须同时查看 `main_data.csv` 和 `sample_metadata.csv` 才能
拼出样本全景，违背了"单一主表"的设计意图。

### 19.2 根因

`_build_minimal_parsed_dataset` 的回退路径在 series_matrix 表达矩阵为空时
**只写表头不写数据行**——`samples` 列表虽然已通过 §18 的
`_recover_samples_from_series_matrix` 恢复，但未传入
`_build_minimal_parsed_dataset`，导致 sample 元数据只流向 `sample_metadata.csv`
而未流向 `main_data.csv`。

设计层面，`main_data.csv` 的 schema 假设每行都是 `gene × sample` 表达值，
没有为"无表达矩阵"场景预留行类型字段。`measurement_type` 列已经存在但
仅用过 `tximport_estimated_count` 一个值，可复用为行类型标记。

### 19.3 修复

**分支**：`fix/main-data-always-populated`

设计原则：**`main_data.csv` 始终至少包含每个样本一行真实数据**，无论 GEO
series 的数据类型如何。表头不变（保持 22 列 schema 稳定），通过
`measurement_type` 列区分行类型：

| `measurement_type` | 行含义 | 出现场景 |
|---|---|---|
| `tximport_estimated_count` | `gene × sample` 表达值 | microarray / 已处理 counts |
| `sample_metadata` | 一个样本一行元数据 | snRNAseq / RNA-seq（matrix block 为空） |

具体改动：

1. `backend/app/pipeline/stages/processing.py`
   - `_build_minimal_parsed_dataset` 新增 `samples: list[GeoSampleMetadata] | None`
     参数。当 `samples` 非空时，为每个样本生成一行
     `measurement_type="sample_metadata"` 的元数据行：
     - `sample_id`/`source_sample_alias`/`dataset_id`/`source_id`/`asset_id`
       正常填充
     - 表达相关字段（`gene_id_raw`/`gene_id`/`gene_id_namespace`/
       `gene_id_version`/`expression_value`/`source_raw_value`）留空
     - `value_semantics="metadata_only"`，`value_scale="na"`，
       `expression_unit="na"`
     - `source_logical_file="series_matrix_metadata"`，
       `source_line_number=0`，`source_column_index=0`，
       `source_column_name="sample_metadata"` —— `source_line_number=0` 是
       "无源行号"哨兵，validation 会据此跳过 lineage 检查
     - `record_id = make_record_id(dataset_id, "sample_metadata", sample_id)`
       保证唯一且确定性
   - `run_processing` 把 `_recover_samples_from_series_matrix` 的结果传给
     `_build_minimal_parsed_dataset`，并更新日志消息（"main_data.csv will
     contain N sample_metadata rows"）。

2. `backend/app/pipeline/stages/validation.py`
   - `_validate_package` 的 `source_value_lineage` 检查跳过
     `measurement_type == "sample_metadata"` 的行：这些行没有
     `expression_value` 可与源文件对照（`source_line_number=0` 也是哨兵）。
   - `source_value_lineage` 检查的 `details` 字段新增 `skipped_metadata_rows`
     计数，便于审计；`checked_count` 改为 `len(sampled_rows) - sampled_skipped`
     （跳过的行不计入"已检查"）。

3. `backend/app/agent_loop/agent.py`
   - system prompt 中"main_data.csv 为 0 行时的处理"小节改写为
     "main_data.csv 数据完整性保证"：
     - 明确告知 LLM `main_data.csv` **不会出现 0 行**（除非 task 本身没有数据源）
     - 列出两种 `measurement_type` 及其含义
     - 要求 LLM 在报告中展示 `main_data.csv` 的实际行数和
       `measurement_type` 分布
     - 重申"不要编造表达值、基因名或路径"——表达相关字段为空就是为空

4. `backend/tests/pipeline/test_geo_tximport_processing.py`
   - `test_minimal_dataset_with_samples_writes_one_metadata_row_per_sample`：
     3 个样本 → 3 行 `sample_metadata` 数据，`row_count=3`（非 0），
     表达字段全空，`source_line_number=0`，`record_id` 唯一。
   - `test_minimal_dataset_without_samples_is_schema_only`：
     `samples=None` 和 `samples=[]` 都生成 0 行 CSV（向后兼容）。
   - `test_validation_skips_lineage_for_sample_metadata_rows`：
     手工构造一份只有 `sample_metadata` 行的 staging package，验证
     `source_value_lineage` 检查 `status="passed"`、`failed_count=0`、
     `details.skipped_metadata_rows=2`。

### 19.4 验证

- `tests/pipeline/test_geo_tximport_processing.py` 9 个测试全部通过
  （含 3 个新增回归测试）
- backend `uv run pytest -q` → 903 passed, 18 deselected
- backend `uv run ruff check app tests` → 0 errors

### 19.5 设计权衡

- **为何不扩展 `_OUTPUT_COLUMNS` 新增 `treatment`/`cell_line`/`organism` 等列？**
  这会让 schema 变宽，破坏现有 fixture 测试和验证规则，且这些字段已经在
  `sample_metadata.csv` 中维护。`main_data.csv` 通过 `sample_id` 与
  `sample_metadata.csv` 做外键关联即可。
- **为何不把 `sample_metadata.csv` 全部内容合并到 `main_data.csv`？**
  会破坏长表 schema（每行不再是 `gene × sample`），且 `sample_metadata.csv`
  与 `main_data.csv` 的语义不同（一个是样本级元数据，一个是测量值长表）。
  保持两表分离 + 通过 `sample_metadata` 行类型让 `main_data.csv` 至少有
  每样本一行的"指针"，是 schema 稳定性和数据完整性的折中。
- **为何 `source_line_number=0` 而非 -1？**
  validation 的 `int(row["source_line_number"]) - 1` 在 `0` 时变成 `-1`，
  会取 `source_lines[-1]`（最后一行）—— 但我们通过
  `measurement_type == "sample_metadata"` 提前 `continue` 跳过整行，
  根本不会执行到 `line_index` 计算，所以 `0` 是安全的哨兵。若未来移除
  `measurement_type` 检查，应改为 `-1` 并显式跳过。

### 19.6 后续建议

- 跨数据源 unification：用户提到"每个脚本/工具/skill 最终产生的 CSV 应该
  格式完全一致，格式适配性应该在工具内得到妥善处置"。当前 Pipeline 只处理
  GEO，其他数据源（PDB/Reactome/GDC/PubChem）由各自 skill 产出独立 CSV。
  后续可考虑为所有 skill 定义统一的 main_data schema 适配器，让 PDB 结构数据、
  Reactome 通路数据等也能以 `measurement_type="structure"`/`"pathway"` 等
  行类型进入 `main_data.csv`。
- `quality_report.csv` 增加 `main_data_row_count_by_measurement_type` 指标，
  让 LLM 和用户都能从结构化字段感知行类型分布。

---

## 附录：本轮审查方法

- 前后端完整启动验证（`uvicorn --reload` + `pnpm dev`）
- 3 个并行 search agent 深度探索（第一轮）
- 2 个并行 search agent 深度探索（第二轮，覆盖 compaction/cancellation/并发/配置/测试）
- 关键代码位置逐行 Read 验证（`runner.py:100-307`、`manager.py:1175-1264`、`agent.py:70-128`、`runner.py:430-459`）
- 与 `docs/TODO.md` 现有条目交叉比对，避免重复
- 与 `project_memory.md` 硬约束逐条核对，标记冲突项
