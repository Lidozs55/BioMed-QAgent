# ===== 后端核心依赖 =====
# Web 框架
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
websockets>=12.0

# LLM 客户端（阿里云百炼 OpenAI 兼容模式）
openai>=1.12.0
httpx>=0.27.0

# 数据处理
pandas>=2.2.0
numpy>=1.26.0
scipy>=1.12.0

# PDF / 文档解析
pdfplumber>=0.10.0

# 网页爬取
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.1.0

# 数据校验
pydantic>=2.6.0

# 可视化（脚本端使用）
matplotlib>=3.8.0

# 浏览器自动化（可选，Acquire 阶段）
# playwright>=1.42.0

# ===== 不再使用（已精简存储层）=====
# qdrant-client   # 向量库 — 已移除
# redis           # 缓存 — 已移除
# aiosqlite       # SQLite 异步 — 已移除（改用内存+文件）

# ===== 可选依赖（按场景安装）=====
# mineru / magic-pdf  # PDF 复杂版面解析
# grobid-client       # 参考文献解析
# rpy2                # 调用 R 分析包
# seaborn             # 可视化
