## Review
- **Blocker (Important): V2 `BuildResult` still does not reach the durable Run contract.** `execute_dataset_build` constructs and returns `result` only in its tool JSON at [dataset_build_tool.py](/mnt/d/code-linux/BioMed-QAgent/backend/app/pipeline/dataset_build_tool.py:241) and [dataset_build_tool.py](/mnt/d/code-linux/BioMed-QAgent/backend/app/pipeline/dataset_build_tool.py:286). It neither installs a pending publication nor calls any Run-level `set_build_result`. Finalization therefore sees no pending publication at [runner.py](/mnt/d/code-linux/BioMed-QAgent/backend/app/agent_loop/runner.py:1271), then the manager emits its fallback `NO_DATA` summary at [manager.py](/mnt/d/code-linux/BioMed-QAgent/backend/app/runtime/manager.py:1829). A successful V2 publication, or B5's structured V2 NO_DATA, can consequently be reported to the frontend as the manager's generic NO_DATA and remains outside the task artifact/publication event path. B9 closes only the V1 tool envelope at [tool.py](/mnt/d/code-linux/BioMed-QAgent/backend/app/pipeline/tool.py:619); it does not close B5 end to end.
- **Important: B5 can falsely classify a partially usable multi-source build as `NO_DATA`.** Any failed operation whose error contains `contains no data rows` makes `_is_no_data_outcome` return true unconditionally at [dataset_build_tool.py](/mnt/d/code-linux/BioMed-QAgent/backend/app/pipeline/dataset_build_tool.py:320). Parsing is per binding at [expression_runner.py](/mnt/d/code-linux/BioMed-QAgent/backend/app/datasets/build/expression_runner.py:135), so one header-only binding aborts the executor even when another binding contains valid rows; the returned result then marks every binding rejected at [dataset_build_tool.py](/mnt/d/code-linux/BioMed-QAgent/backend/app/pipeline/dataset_build_tool.py:241). The new test covers only a single empty source, not this mixed-source case.
- **Important: D2 is only partially closed; heavy V2 operations still block cancellation.** The branch passes the cancellation token, but synchronous `adapter.parse(...)` still executes directly inside an async handler at [expression_runner.py](/mnt/d/code-linux/BioMed-QAgent/backend/app/datasets/build/expression_runner.py:135), and the executor directly awaits that handler at [executor.py](/mnt/d/code-linux/BioMed-QAgent/backend/app/datasets/runtime/executor.py:350). The event loop cannot process a cancellation request during a large parse/canonicalize/integrate operation. This contradicts the consolidated claim that the synchronous parsing half of D2 was fixed at [REVIEW_2026-08-08-phase4-bug-sweep.md](/mnt/d/code-linux/BioMed-QAgent/docs/REVIEW_2026-08-08-phase4-bug-sweep.md:27).
- **Minor: A8 persistence does not migrate existing Phase 4 snapshots.** New snapshots persist `_artifact_ids_by_run`, but snapshots written before this branch already contain `artifact_count` and lack the private map. The loader restores the map only when present at [repository.py](/mnt/d/code-linux/BioMed-QAgent/backend/app/runtime/repository.py:607), while historical reconstruction is gated on the unrelated absence of `artifact_count` at [repository.py](/mnt/d/code-linux/BioMed-QAgent/backend/app/runtime/repository.py:620). `TaskIndex` repeats the same logic at [index.py](/mnt/d/code-linux/BioMed-QAgent/backend/app/runtime/index.py:435). After upgrade, replaying an old artifact ID increments the count again at [state.py](/mnt/d/code-linux/BioMed-QAgent/backend/app/runtime/state.py:397). The added repository test starts with a newly saved snapshot, so it does not exercise this upgrade path.
- **Note: C1 is correctly accepted, but C2 exposes a remaining backend/frontend event-contract mismatch.** Backend contracts include `operation_started/progress/completed/failed` at [events.py](/mnt/d/code-linux/BioMed-QAgent/backend/app/domain/contracts/events.py:65), while the frontend allowlist ends without them at [transport.ts](/mnt/d/code-linux/BioMed-QAgent/frontend/src/runtime/transport.ts:16). If production wiring starts emitting these already-defined events, transport drops sequence N; C2 detects N+1 and replays once at [transport.ts](/mnt/d/code-linux/BioMed-QAgent/frontend/src/runtime/transport.ts:729), but N remains permanently undeliverable and the task cursor stalls. This is latent today because `execute_dataset_build` does not wire an event sink, but it is not coherent with the backend contract.
- **Note: the claimed count mixes finding IDs with implementation groups.** The reports contain 23 implementation groups: wave 1 backend 8, wave 1 frontend 7, wave 3 backend 6, wave 4 frontend 2. However, the `B3+B4` group at [REVIEW_2026-08-08-phase4-bug-sweep.md](/mnt/d/code-linux/BioMed-QAgent/docs/REVIEW_2026-08-08-phase4-bug-sweep.md:49) covers two original findings. Thus 23 groups cover **24 finding IDs**, and the deferred table contains **6 IDs** (`A2/A3/A4/A6/A7/C7`), not 7. The headline's “23 findings fixed / 7 deferred” arithmetic at lines 5-6 is inaccurate.
- **Correct:** B3+B4 now preserve manifest-relative paths and verify artifact size/hash plus exact provenance asset IDs before promotion. C1's `publication_created` transport acceptance and C2's contiguous reducer guard are correctly implemented for supported event types. Fresh post-branch snapshots preserve A8 dedup state across repository reloads.

## Residual Risks
The explicitly deferred A2/A3 publication transaction issues, A4 restart/HIL invalidation, A6 lock/checkpoint retention, A7 unbounded active-task listing, and C7 artifact ownership remain. Wave 3 also records the pre-existing missing `validation_result_ref` target and the best-effort D1 race. No controller-verified quality gate was rerun, per instruction; reported `2377` backend and `677` frontend results were treated as attestations only.