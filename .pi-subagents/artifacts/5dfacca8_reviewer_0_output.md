## Review
- **Important, G3 / B3+B4 NOT ADDRESSED:** `DatasetPublication.validation_result_ref` advertises `validation_report.json` at `backend/app/datasets/build/expression_runner.py:359-366`, but the manifest inventory is assembled without that report at `backend/app/datasets/build/manifest.py:233-241`, and publication copies only `manifest.artifacts` plus the manifest/publication records at `backend/app/datasets/build/expression_runner.py:375-394`. The immutable version therefore contains a dangling required reference, and the report is outside the size/SHA-256 gate at `backend/app/datasets/build/invariants.py:145-185`. Relative paths and declared artifact hashes were fixed, but publication integrity is still incomplete.
- **Important, B5 REGRESSION:** the two NO_DATA signals are not scoped to the current attempt. The string signal returns immediately without checking row count or publication state at `backend/app/pipeline/dataset_build_tool.py:320-321`; the manifest signal reads the reused build workspace at `:322-337`, while that workspace is stable by `build_id` at `:197-200`. Consequently, a stale zero-row manifest can convert a later unrelated failure into NO_DATA, while a genuine zero-row manifest after an earlier successful publication becomes a generic retryable error because *any historical* version directory exists. In a multi-source parse failure, the response also marks every binding rejected at `:241-249`, even if earlier bindings parsed successfully.
- **Important, D1 NOT ADDRESSED:** exclusivity remains an entry-only race. The build checks `main_input_pending` once at `backend/app/pipeline/dataset_build_tool.py:117-130`; the correction coroutine does not set the flag until `backend/app/agent_loop/main_input_broker.py:152`, and there is no recheck before publication at `backend/app/datasets/build/expression_runner.py:314-394`. If the concurrently scheduled build reaches line 118 first, the original same-turn HIL/publication scenario still occurs.
- **Important, D2 NOT ADDRESSED:** cancellation tokens are now wired at `backend/app/pipeline/dataset_build_tool.py:210-230`, but the original event-loop responsiveness defect remains. Source hashing uses synchronous `read_bytes()` at `:172-175`, and async operation handlers directly call synchronous parse/canonicalize/integrate work at `backend/app/datasets/build/expression_runner.py:135-179` and `:211-221`. While those calls run, the loop cannot process the cancel request or start the D1 correction coroutine; token checks only happen between operations at `backend/app/datasets/runtime/executor.py:478-488`.
- **Important, A1 REGRESSION:** if appending the fallback `RunCancelledPayload` itself fails, `_handle_worker_failure` unconditionally removes live ownership in `finally` at `backend/app/runtime/manager.py:1623-1635`, then swallows/logs the failure at `:1643-1648`. The run remains `CANCEL_REQUESTED`, but subsequent cancellation rejects it because no live execution exists at `:1142-1144`. The added test only makes `RunFailedPayload` persistence fail while allowing `RunCancelledPayload` to persist, so terminalization is not guaranteed.
- **Minor, A8 NOT ADDRESSED:** counting now deduplicates only by artifact ID at `backend/app/runtime/state.py:391-404`; a same-ID event with a conflicting hash/path is silently accepted, contrary to the original conflict repro. Upgrade compatibility is also incomplete: snapshots lacking `_artifact_ids_by_run` restore an empty map at `backend/app/runtime/repository.py:607-612`, and journal reconstruction occurs only when `artifact_count` itself is absent at `:620-635`. A pre-fix snapshot with an existing count therefore increments again on the next duplicate event.
- **Important, B8 REGRESSION:** chronological selection works for generated records, but `datetime.fromisoformat` accepts timezone-naive timestamps at `backend/app/datasets/build/expression_runner.py:481-496`; mixing one naive record with normal timezone-aware records makes `max(...)` at `:499` raise `TypeError`. Such a malformed record is not skipped as claimed and blocks publication/tool result selection.

## Status Matrix
- **B1 ADDRESSED:** strict validators at `backend/app/datasets/contracts.py:132-168`; containment guard at `backend/app/pipeline/dataset_build_tool.py:65-81`.
- **B2 ADDRESSED:** sorted source SHA-256/size closure is included for every operation at `backend/app/datasets/runtime/executor.py:503-519` and supplied by the tool at `backend/app/pipeline/dataset_build_tool.py:219-230`.
- **B7 ADDRESSED:** artifact and manifest hashing is chunked at `backend/app/api/routes.py:900-918` and used by list/download checks at `:834-892`.
- **B8 REGRESSION:** normal time ordering fixed, malformed naive/aware timestamp mixture crashes at `backend/app/datasets/build/expression_runner.py:481-499`.
- **B9 ADDRESSED:** V1 envelope includes `manifest.build_result` at `backend/app/pipeline/tool.py:619-624`.
- **D2 NOT ADDRESSED:** cooperative token propagation is present, but synchronous heavy work still prevents prompt cancellation at the locations above.
- **D3 ADDRESSED:** finite `[1, 3600]` validation at `backend/app/agent_loop/request_human_correction.py:60-66` and rejection before broker invocation at `:116-129`.
- **A8 NOT ADDRESSED:** duplicate counts are fixed only for migrated/new bookkeeping; conflicting identities and pre-fix snapshot migration remain open at the locations above.
- **A1 REGRESSION:** normal fallback terminalizes, but fallback persistence failure now drops live ownership while leaving a nonterminal run.
- **A5 ADDRESSED:** parent state is loaded under the task lock and terminal parents are rejected before broker routing at `backend/app/runtime/manager.py:1297-1333`.
- **B3+B4 NOT ADDRESSED:** declared artifacts are path-preserved and hashed, but `validation_result_ref` remains dangling and outside inventory/integrity coverage.
- **B5 REGRESSION:** fresh header-only input works, but dual-signal attempt scoping is incorrect.
- **B6 ADDRESSED:** parsed row width is checked before field validation at `backend/app/datasets/build/profiles.py:271-310`.
- **D1 NOT ADDRESSED:** the entry check is best-effort and does not close the concurrent start race.

No controller-verified gates or tests were run; the worktree was not modified. Read-only inspection showed no staged files, with only pre-existing untracked review/runtime scratch paths.