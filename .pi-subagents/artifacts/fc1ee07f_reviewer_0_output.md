## Review
- **Important, C3 remains incomplete:** selecting a terminal history task creates a permanent live subscription. `performSelectionHandoff` always calls `subscribe` after snapshot hydration at `frontend/src/runtime/controller.ts:240-260`, while `subscribe` unconditionally inserts the task into `desired` at `frontend/src/runtime/transport.ts:436-449`. `shouldSubscribe` is consulted only after an event is applied at `frontend/src/runtime/transport.ts:710-742`; a terminal task subscribed after its latest event receives no later frame, so reconciliation never runs. Repeatedly viewing completed tasks recreates the subscription accumulation C3 intended to fix.
- **Important, C2 is only transient-gap safe:** an unknown or permanently schema-incompatible event is dropped at `frontend/src/runtime/transport.ts:137-142,220-225`. The next valid event records a gap and triggers recovery at `frontend/src/runtime/transport.ts:703-733`, but `gapRecoveryCursors` permits only one forced recovery per cursor and is not cleared by socket replacement or natural reconnect (`frontend/src/runtime/transport.ts:372-379,589-610,643-674`). Replay therefore repeatedly drops the same bad event and all later durable state remains frozen. There is no tight reconnect loop, but there is also no REST-snapshot fallback, visible incompatibility state, or terminal escape.
- **Minor, C8 is not time-reactive:** expiry is calculated only during render at `frontend/src/components/UserInputDialog.tsx:162-167`; no timer schedules a render at the deadline. If the dialog renders before expiry and remains idle past it, the click handler still closes over `expired === false` at `frontend/src/components/UserInputDialog.tsx:176-188`, and buttons remain enabled at `frontend/src/components/UserInputDialog.tsx:436-485`. Existing tests cover already-expired and future-at-render cases, not crossing the deadline while mounted (`frontend/src/test/user-input-dialog.test.tsx:878-942`).
- **Blocker:** none.

## Scoped Verdicts
| ID | Verdict | Current evidence |
|---|---|---|
| C1 | Correct | `publication_created` is accepted at `frontend/src/runtime/transport.ts:16-43`, reduced at `frontend/src/runtime/reducers/index.ts:108-110`, and exercised end-to-end at `frontend/src/test/agent-stream.test.ts:275-320`. |
| C3 | Partial | Live terminal events unsubscribe correctly at `frontend/src/runtime/transport.ts:710-751`, but terminal snapshot selection leaks as described above. |
| C4 | Correct | Owning cancellation clears the prompt at `frontend/src/runtime/reducers/runtime.ts:324-333`; dialog actions additionally require `awaiting_user_input` at `frontend/src/components/UserInputDialog.tsx:149-188`. |
| C5 | Correct | Resume transitions require matching run/request and awaiting status at `frontend/src/runtime/reducers/hil.ts:51-84`. |
| C8 | Partial | Initial past/invalid deadlines are handled, but mounted dialogs do not age into expiry. |
| C9 | Correct | Header-only CSV is empty when `rows.length === 0` at `frontend/src/components/ResultsViewer.tsx:177-191`. |
| C10 | Correct | Artifact role reaches `fileType` at `frontend/src/components/ArtifactSheet.tsx:93-97`. |
| C2 | Partial | Contiguous transient replay works; permanent incompatible events freeze the cursor. |
| C6 | Correct | Build summary/action, stable failure details, and cancellation stage render at `frontend/src/components/ChatPanel.tsx:134-170,496-527`. |

## Deep Checks
- **C1 + C2:** the normal publication sequence is now contiguous and no longer triggers C2. A malformed publication can still enter the permanent-gap path because publication payloads use the permissive default validator at `frontend/src/runtime/transport.ts:271-340`, while reducer linkage checks can throw at `frontend/src/runtime/reducers/runtime.ts:400-415`.
- **Continuation:** `continueTask` adds the queued run before subscribing from the retained watermark at `frontend/src/runtime/controller.ts:496-525`. Fully hydrated tasks replay from the prior cursor; summary-only tasks are reset to sequence 0 at `frontend/src/stores/agentStore.ts:202-215`. `addAcceptedTask` clears an obsolete prompt at `frontend/src/stores/agentStore.ts:184-200`.
- **Pending prompt hydration:** active `awaiting_user_input` tasks remain subscribable (`frontend/src/runtime/reducers/shared.ts:22-31`; `frontend/src/hooks/useAgentStream.ts:21-22`). Selection replays events before snapshot hydration at `frontend/src/runtime/controller.ts:240-259`, and hydration preserves a replayed prompt only when the same run remains awaiting at `frontend/src/runtime/reducers/shared.ts:425-430`. The focused prompt test at `frontend/src/test/runtime-controller.test.ts:692-801` preserves this intent.
- **Six wave-4 fixture adjustments:** no loss of asserted intent found. The store seeds at `frontend/src/test/store.test.ts:171,195,305`, terminal-state seed at `frontend/src/runtime/__tests__/terminal-state.test.ts:21-32`, duplicate/stale seed at `frontend/src/test/runtime-reducer.test.ts:590-621`, re-entry sequence at `frontend/src/test/runtime-reducer.test.ts:1586-1668`, reconnect range at `frontend/src/test/agent-stream.test.ts:981-1002`, and controller replay fillers at `frontend/src/test/runtime-controller.test.ts:1993-1998,2621-2627` replace impossible jumps with contiguous history while retaining each original classification, ordering, watermark, and generation assertion.