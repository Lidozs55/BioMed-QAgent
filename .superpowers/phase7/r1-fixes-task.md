# Round-1 fix worker task (parent-orchestrated review loop, Phase 7)

You are applying the Round-1 synthesized fixes for Phase 7 in /mnt/d/code-linux/BioMed-QAgent (branch main @ 94316f4, working tree clean). Work on a new branch: `git checkout -b fix/phase7-r1-fixes`. You are the ONLY writer on this repo — commit your work, do NOT push. TypeScript strict (no `as any` / `@ts-ignore`), Python type-annotated, TDD red-first for each fix (write the failing test first, watch it fail for the right reason, then fix, then green).

## THREE FIXES (apply in this order, separate commits)

### F1 — backend `list_builds` must survive corrupt manifests (R1C-01)
File: `backend/app/api/routes.py` (list_builds ~:1241-1262, `_load_build` raises 409 for corrupt/truncated dataset_manifest.json). One corrupt manifest currently takes down the whole `GET /api/v1/builds` listing. Every other list surface (cache.list, list_legacy) skips corrupt entries. FIX: in the list loop, catch the corrupt-manifest failure and continue (skip that build); list still returns the healthy builds. TDD: red test = seed two builds, corrupt one manifest (truncate JSON), assert `GET /builds` returns 200 with the healthy build and omits the corrupt one (before fix it 409s). Keep 404 semantics of `GET /builds/{build_id}` for a corrupt build intact (detail endpoint may still fail loudly).

### F2 — frontend: suppress stage/progress timeline items for runs carrying operation events (R1S-01)
Real bug (verified by parent): in managed runs (run_id != null) the reducer projects THREE timeline rows per stage — kind:"stage" (`src/runtime/reducers/pipeline.ts` :216/:238 via applyStageEvent), kind:"operation" (:85 via applyOperationEvent), kind:"progress" (`src/runtime/reducers/stream.ts` :912/:932 via applyStageProgress). Docs (`docs/ARCHITECTURE.md` §17.2) mandate the frontend renders by operation identity during the compat period. FIX (reducer-level, deterministic): for a given run_id, once operation events are present (or: when applying stage_*/progress events whose run has any operation item), do NOT create/keep stage or progress items for that run — operation items are the richer superset (label/category/status/error). Legacy replay (pre-T3 events.jsonl, no operation events) MUST still render stage/progress items unchanged. TDD: red test in `src/test/runtime-reducer.test.ts` (or a new file) — feed a managed-run event sequence [stage_started, operation_started, operation_completed, stage_completed, stage_progress] and assert exactly ONE timeline item (operation) is projected; and a legacy-run sequence [stage_started, stage_completed, stage_progress] (run_id null OR no operation events) still yields stage+progress items. Read `src/runtime/reducers/pipeline.ts` + `stream.ts` + existing reducer tests to match conventions. Keep ALL existing tests green — check whether any existing test asserted the old dual-row behavior and update only if its premise was the bug (document why).

### F3 — frontend: `useTaskBuildId` must refetch when a NEW run produces a build (R1S-02)
File: `frontend/src/hooks/useTaskBuild.ts`. Bug: the hook key is the template `taskId:hasBuildResult` and effect deps don't include the latest run id — after a 2nd build-producing run, both key components stay true, the effect never refetches, and the viewer keeps showing the FIRST run's build. FIX: include the latest run id (whatever the hook already reads — find how it derives build_id; there is likely a runs selector; add latestRunId to the memo key + effect deps) so a new run triggers a refetch. TDD: red test — render hook with a task whose run list advances from run A (build X) to run B (build Y); assert the hook re-fetches and resolves build Y (before fix it stays X). Look at how existing hook tests are written (`frontend/src/test/build-results-viewer.test.tsx` uses useTaskBuild indirectly; find the closest test seam — you may test via the viewer or extract a pure helper if cleaner, but keep it minimal).

## CONSTRAINTS
Surgical diffs only; no gold-plating; do not touch unrelated files; do not modify backend except routes.py for F1; keep existing 2718 backend / 712 frontend tests green (you will add ~6-10 new).

## GATES (evidence required in report)
- backend: `cd backend && source .venv/bin/activate && pytest -q` (expect 2718 + F1 tests, 2 skipped, 28 deselected), `ruff check app/ tests/ launcher.py` (clean), `python -c "import app.main"` OK.
- frontend: `cd frontend && pnpm lint` (0) && `pnpm tsc` (0) && `pnpm test` (expect 712 + F2/F3 tests) && `pnpm build` OK.

## REPORT (in-chat, <200 words)
Status, branch + commit hashes, test summary before/after, exact gate outputs, any surprises (esp. existing tests whose premise was the F2 bug), and anything left undone. If any fix turns out to be mis-specified after reading the code (e.g. F2 premise wrong), STOP and report rather than force a wrong implementation.
