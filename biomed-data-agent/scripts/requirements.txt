# BioMed Data Agent Skill - Python Dependencies
# Install in sandbox: pip install -r scripts/requirements.txt

# === Core HTTP / Parsing (Stage 1-3) ===
requests>=2.28.0          # All API clients use requests
beautifulsoup4>=4.11.0    # HTML parsing for fallback crawl
lxml>=4.9.0               # XML parsing for NCBI E-utilities responses

# === PDF Parsing (Stage 3) ===
pdfplumber>=0.9.0         # PDF table extraction (used by pdf_table_parser.py)

# === Data Cleaning (Stage 4) ===
PyYAML>=6.0               # Load dictionaries/*.yaml and schemas/*.yaml

# === Bioinformatics Analysis (Stage 5) ===
numpy>=1.22.0             # Numeric operations
scipy>=1.9.0              # Statistical tests
statsmodels>=0.13.0       # BH FDR correction (optional, has built-in fallback)
networkx>=2.8             # PPI network analysis

# === Visualization (Stage 7) ===
matplotlib>=3.6.0         # All chart generation
seaborn>=0.12.0           # Heatmap clustermap

# === Optional - Image Understanding ===
# extract_chart_data.py uses requests + OpenAI-compatible API (no extra dep)
# Requires QWEN_API_KEY environment variable

# === Optional - Not installed by default ===
# Biopython is NOT required; PDB/FASTA parsers use pure Python
# GEOparse is NOT required; SOFT parser uses pure Python
# rpy2 is NOT required; analysis uses Python stats
