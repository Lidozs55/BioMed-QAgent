"""BioMed-QAgent Windows GUI launcher.

A pywebview desktop shell around the exact same production entrypoint
``start.bat`` runs (``node server/dist/index.js --static``). See
``packaging/windows/README.md`` for the build and test workflow.
"""
